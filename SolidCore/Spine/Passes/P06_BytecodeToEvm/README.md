# P06 Bytecode To Evm

This pass connects bytecode artifacts to the public EVM semantics under the
boundary conditions required by the final target model.

The authoritative interface is `Interface.lean`; architectural intent lives in
`ARCHITECTURE.md`.
