# SolidCore

`SolidCore` is the public compiler side of the project. The root module imports
`SolidCore.Spine`, which is the only public compiler route.

`Solidity/` contains reusable source-language syntax and semantics. `Spine/`
contains the layer interfaces, adjacent passes, and theorem spine.
