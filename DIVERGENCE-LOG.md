# Divergence Log — solidity-lean vs pinned solc 0.8.35 (legacy)

Chronological log of every model↔solc divergence found during the semantics-hardening loop.
**Newest first.** Each row is added as soon as a searcher confirms the divergence (before/at fixer dispatch), then its status is updated through merge.

- **Class:** `over-reject` (model rejects, solc accepts) · `over-accept` (model accepts, solc rejects) · `wrong-value` (both accept, model produces wrong runtime result) · `verified-correct` (investigated, model already matched — locked in with witnesses)
- **Reach:** `replay` (reachable via the fixture importer — over-rejects & wrong-values) · `accept-only` (acceptance-predicate/contest only — pure over-accepts)
- **Status:** `found` → `fixing` → `built` (green in worktree, awaiting merge) → `merged` (on main, gated)

_Log opened 2026-07-10 07:xx PDT. Rows above #52 predate this log; see the task tracker (#13–#51) for the full earlier history._

| Found (PDT) | # | Divergence | Class | Reach | Status |
|---|---|---|---|---|---|
| 2026-07-12 19:xx | 205 | WS1-ENV-AUDIT-BATCH (6 NEW narrow-cleanup divergences found by WS1's 32-probe env-audit battery, all EVM-verified 32/32 Forge PASS, uint8 a=200 b=100): env-less PRIMARY lowering positions beyond #201 that skip narrow (uintN<256) checked-arith Panic 0x11. (1) `new uint256[](a+b)` → EVM Panic 0x11, model RET 300 (env `Expr.newExpr` arm + vardecl/return reroute). (2) `this.sink(a+b)` external-call arg → EVM Panic 0x11, model wrong revert (callee-side decode) — fixed by threading optional `argEnvLower` through the whole `externalCallAbi…/toExternalCall…WithReturns/SingleReturn/Return/Discard/AssignVars/AssignBindings` chain (Lean default-arg ⇒ zero churn) at 34 dispatcher sites. (3) `try this.sink(a+b)` → EVM Panic 0x11 UNCAUGHT, model RET 99 (catch swallowed it) — same fix. (4) `addmod(a+b,1,7)` → EVM Panic 0x11, model RET 0 (=(300+1)%7) (env addmod/mulmod arm). (5) `arr[arr[a+b]]` → EVM Panic 0x11, model Panic 0x32 OOB (flag recursion into `Expr.index` + wide-key gate). (6) `uint8[2] t=[a+b,1]` → EVM Panic 0x11, model OVER-REJECT whole contract (env `Expr.array` arm + new `Exprs.toCoreAsListWithEnvFuel?`). Fns in Interface.lean: `Expr.abiArgNeedsEnvCleanupFuel?` (~7935), `abiBuiltinArgsNeedEnvCleanup` (~8016), `toCoreAsWithEnvFuel?` (~8420-8700), `externalCallAbiWithKindEnv?`+chain (~6100-6460). Controls green (already env-aware): named emit/revert args, struct ctors, tuple assign/decl, lib/fn-ptr/modifier args, push(a+b), for-init, delete m8[a+b], indexed/two-phase emit. Class: wrong-value/missing-panic + one over-reject; replay-reachable | wrong-value/over-reject | replay | MERGED pending batched gate (main c3c0073..403390d; WS1 env-audit; folds in #201 + #196 gensym; full build green 1147 jobs, NO proof change; 65-guard witness + 2 Forge lanes 61 tests + 12-lane self-gate green; batched WS2+WS3+WS1 replay GREEN 265/265 — Program-2 rearch complete & locked) — GATED |
| 2026-07-12 10:2x | 204 | PUSH-AFTER-DANGLING-WRITE-DEEP-CLEARS (WS2 follow-up residue, newly observable after storage-ref early-binding lands): the model's dynamic-array `push()` DEEP-CLEARS the newly-grown element's slot; the real EVM does NOT zero it — a prior dangling write to that slot (past the old length) survives the re-grow. `uint[] arr; S storage p=arr[i]; arr.pop(); p.a=5; arr.push();` then read `arr[i].a` → EVM 5 (the dangling write persists; push only bumps length), model 0 (push zeroes). Also reachable via adoption-planted words sitting past a length. EVM-verified. Only observable once #146/#177 early-binding makes dangling writes actually land (WS2). Root: `push()` implementation zero-initializes the new element instead of leaving the slot's existing word. Class: wrong-value; replay-reachable. **WS2 LANDED on main (8b10a95) — hold released; now live+observable (dangling writes land under early-binding).** Fix: `push` must bump length + return the (possibly non-zero) existing slot, not clear it. Flagged in STORAGE_REF_DESIGN.md §137-144 as WS2's own known-residue. | wrong-value | replay | found (UNBLOCKED — WS2 landed; Fable fixer actionable now) |
| 2026-07-12 10:1x | 203 | STORAGE-DYNAMIC-VALUE-MATERIALIZATION-COMPLETENESS (4 groups M1–M4, sibling of #192/#202; completeness sweep after #192/#202): the lowering-side rewriter `materializeStorageValueUseCore` (`Interface.lean:4359`, `Expr.storage → Expr.storagePath key []`) is NOT applied at four more value-use boundaries, so a bare storage `T[]`/`bytes`/`string` lowers to `Expr.storage key` whose eval yields the LENGTH word (or `typeMismatch` for string) → model Panic 0 where EVM succeeds. All EVM-verified (8/8 Forge PASS + anvil/cast), importer-accepted → replay-reachable. **M1 EMIT (data AND topics):** `emit E(arr|bs|ss)`, mixed `emit EMix(1,bs,2)`, indexed `emit EIdx(bs|arr|ss)` → panic 0 vs full ABI data + correct topic hashes. Root: `Stmt.emitEvent` lowering `Interface.lean:7211-7213`/:7223-7226 use plain `Args.toCoreExprs?` (no materialize); interpreter arm :9401-9435 also never materializes before `Runtime.emitEvent` (`EventDecl.encodeFields?`:5437 is pure, can't read storage). **M2 abi.decode of storage bytes:** `bs=abi.encode(42); abi.decode(bs,(uint256))` → panic 0 vs 42. Root: :4786-4791 lowers `dataCore` unmaterialized. **M3 wrapper shapes (ternary) defeat the rewriter at ALL wired boundaries:** `keccak256(c?bs:bs2)`/`abi.encode(c?bs:bs)`/`emit E(c?bs:bs2)` → panic 0. Root: `materializeStorageValueUseCore` (:4359-4363) rewrites only a TOP-LEVEL `Expr.storage`; a ternary core passes through → branch = length word. Must recurse through ternary at both env-less (:4481-4951) and env-aware (:8386-8476) sites. **M4 external-call / constructor args of storage ARRAY type:** `this.acceptArr(arr)` (closed-world reachable — strongest repro) → panic 0 vs 2; `ISink(t).f(arr)`; `new Child4A(arr)`; nested `uint[][]`. Root: external-call arg lowering (`externalCallAbiWithKindEnv?`/`externalCallStoragePointerPayload?` ~:6126-6164, :8640-8695) + the 4 ctor-arg `Expr.abiEncode` sites (:4697,:4712,:6215,:6243) lower array idents via plain `toCore?`→`Expr.storage`; `toCoreStorageArrayAs?` (:5330) exists but only reached from `toCoreAs?` (:5391), not these arg paths. UNIFYING FIX (one patch class): apply `materializeStorageValueUseCores` at emit/abi.decode/ctor-arg/external-call-arg sites + make `materializeStorageValueUseCore` recurse through ternary. Asymmetry isolation (ALL MATCH): storage bytes/string args (rescued by `bytesStringSpecialCore?`:4319), struct-whole args even with dynamic members, memory copies, RETURN of storage dynamics in every shape (return path materializes independently — pins M3 to the rewriter not ternary eval), keccak/sha256/erc7201 direct storage args (#192 holds), all abi.encode*/concat DIRECT args incl arrays, low-level `.call(bs)` payload. DISTINCT from #202 (interpreter-side revert arms) — this is lowering-side. M4 overlaps WS2 (external-call arg + storage refs) → **WS2 LANDED on main (8b10a95); M4's WS2 hold released, but M4 stays OOS in v1 via X-EXTCALL (external-call/ctor-arg args) — live only for v2**. M1/M2/M3 fixable now (M1/M3 confirmed live: d203/d203m1/d203m3 → SOUNDNESS_GAP; M2 live: d203m2 → SOUNDNESS_GAP). | over-reject/wrong-value/spurious-revert | replay | found (M1/M2/M3 CONFIRMED live + staged in contest/outbox; M4 unblocked by WS2 but OOS via X-EXTCALL in v1) |
| 2026-07-12 09:4x | 202 | STORAGE-DYNAMIC-VALUE-AT-REVERT-BOUNDARY (sibling of #192): bare storage `T[]`/`bytes`/`string` as a custom-error arg or require-reason produces wrong revert data. `uint[] arr; arr.push(11);arr.push(22); revert EArr(arr);` → model `RevertData.custom "EArr" [word 2]` (the LENGTH word); EVM full `EArr([11,22])` (164-byte). `bytes bs=hex"a1b2c3"; revert EBytes(bs)` → `[word 3]` vs full. `string ss; revert EStr(ss)` → model `RevertData.panic 0` (WRONG KIND); EVM `EStr(...)`. `require(false, ss)` → panic 0 vs `Error("...")`. `require(false, EBytes(bs))` (0.8.26 form) → length-word bug. Secondary value-corruption: `try …catch(bytes d)` where target reverts `EArr(storage arr)` → model d=empty (malformed custom fails encodeRevertData?), EVM d=full 164-byte. Class: wrong-revert-data + wrong-revert-kind + wrong-value; replay-reachable (importer accepts). Root: R3's `Runtime.materializeForValueUse` (`Interpreter.lean:4423-4437`) was wired into keccak/hash/abi.encode* builtin sites (:6882-7023) but NOT the revert arms `Stmt.revert` (:9395-9400, evalList→custom) and `Stmt.revertErrorExpr` (:9385-9394, errorStringBytesRevert? can't read storage ref→typeMismatch). Require-custom-error reaches same Stmt.revert arm (lowering Interface.lean:7237). Fix: materialize each value in both arms (the #192 treatment). Interpreter-side, DISTINCT from #201-E (lowering-side narrow-arith). Isolation (MATCH): storage STRUCT arg even with dynamic bytes member; fixed-size `uint[2]`; memory-copy `uint[] memory m=arr; revert EArr(m)`; calldata bytes/string/array/slice — only TOP-LEVEL storage dynamic-sized values fail. Broad revert/error/bubbling/try-catch/Error-string/Panic-code/require-timing surface otherwise ALL MATCH. **NOTE: the scalar-only sub-case is contest-live, but the flagship `uint[]`/dynamic-array custom-error arg is OOS via X-RETABI (dynamic array not in the faithfully-comparable revert-channel ABI subset — confirmed by d202 → REJECTED_OOS).** | wrong-value/wrong-revert-kind | replay | found (**WS2 LANDED on main (8b10a95) — hold released; Fable fixer actionable now**; but dynamic-array revert-arg repro is OOS via X-RETABI — pick a scalar/bytes repro to make it contest-creditable) |
| 2026-07-12 09:2x | 201 | BUILTIN-ARG-NARROW-CLEANUP-RESIDUE (14 instances, Stage-D #193/#194 incomplete across statement positions): builtin/abi.encode* args + lvalue keys with narrow (uintN<256) checked arith skip Panic 0x11 in MANY positions the #193/#194 return-only fix didn't cover. All EVM-verified (uint8 a=200,b=100; 27/27 Forge tests Panic; importer accepts → replay-reachable). Groups: **A assignment-RHS member-call builtin** `z=abi.encode(a+b)`/`bytes.concat(bytes1(a+b))`/`s=abi.encodePacked(a+b)` (Interface.lean:17006 assign-member-call arm tries only externalCallSingleReturnCoreWithKindEnv? then env-less; never abiBuiltinArgsNeedEnvCleanup/assignmentCoreWithEnv?; ident-call `h=keccak256(...)` MATCHES via :17034/17058). **B vardecl-init** `bytes memory z=abi.encode(a+b)`/`bytes32 h=keccak256(abi.encodePacked(a+b))` (specific ident/member-call vardecl arms ~17690-17742 intercept before generic varDeclCoreWithEnv? :17749). **C require `.length`-of-builtin** `require(abi.encode(a+b).length>0)` (comparison conds ride env-aware bool operands :8003 but `.length`-of-builtin cond shape has no env arm). **D emit builtin+pure args** `emit EB(abi.encode(a+b))`/`emit EMix(a+b, bump())` (bump runs where EVM panics first — #195 emitTwoPhaseHoist? :14126 hoists only CALL args; pure/builtin args stay env-less). **E revert custom-error arg** `revert Err(abi.encode(a+b))` → wrong revert DATA (Stmt.revertCall :7227-7249 env-less). **F nested builtin-in-builtin** `return keccak256(bytes.concat(abi.encode(a+b)))` (a RETURN-position miss!) — `Expr.abiArgNeedsEnvCleanup?` (:7907) matches only arith/narrow-neg/bytesN-cast, not a nested abi.*/concat CALL arg; flag needs ONE more recursion level. **G compound-assign + delete lvalue keys** `arr[a+b]+=1`/`delete arr[a+b]` (state-corrupting; toCoreLValueWithEnv? :15743 wired only into plain-assign; compound/delete use env-less toCoreLValue?). MATCH controls (fixed): return abi.encode(a+b), sink(abi.encode(a+b)) fn-args, st.a[a+b]=v / m[k1][a+b]=v plain-assign, emit-two-phase-call-schedule, literal-fold. UNIFYING FIX: apply abiBuiltinArgsNeedEnvCleanup reroute in the assign/vardecl/emit/revert/require-length arms + extend the flag one recursion level + wire toCoreLValueWithEnv? into compound-assign+delete. This IS REARCH-2 WS1 made concrete (env-less arms leak everywhere) — targeted broad-wiring Fable fixer now; full Stmt-dispatch unification later | wrong-value/missing-panic | replay | found (queued — Fable fixer; broad env-aware wiring, WS1 precursor) |
| 2026-07-12 09:1x | 200 | CONTRACT-QUALIFIED-INHERITED-MEMBER: contract-name-qualified member access resolves through the named contract's INHERITED scope; solc restricts it to members declared DIRECTLY in the named contract. `contract Base{uint internal constant K=5;} contract C is Base{ f()returns(uint){return C.K;} }` → solc REJECTS (Error 9582 "Member \"K\" not found ... in type(contract C)"); model ACCEPTS + returns 5. 7 instances (all solc-reject / model-accept): `C.K` (inherited constant via derived's own name), `C.v` (state var), `emit C.Ev(1)` (event), and via an INTERMEDIATE `B` (`contract B is A{}`): `B.K`/`B.v`/`emit B.Ev`/`revert B.E(3)`. Root: `TypeContext.contractDataMemberTy?` (`TypeCheck.lean:~5829`), `contractSelectorMemberTy?` (~5847), `contractErrorSig?` (~5872) all search `contractDispatchOrder` (full C3 own+inherited) — should be DIRECT-declarations-of-the-named-contract only. Fix: replace dispatch-order findSome?/any with direct-members-only lookup in those 3 helpers. Isolation: `Base.K`/`Base.v` (declared in named contract) both ACCEPT+correct; `A.K` from grandchild both ACCEPT; `C.K` with K in C itself both ACCEPT; FUNCTIONS + ENUMS already correct (`C.h()`/`C.E.X` rejected by both — only data/selector/error helpers wrong). Broad inheritance-scope sweep otherwise ALL MATCH (using-for/super-C3/private-internal/shadowing/modifiers/construction/storage-layout) | over-accept | accept-only | found (BACKLOG — accept-only per over-accept-reachability rule; precise root, small fix) |
| 2026-07-12 08:3x | 199 | MODIFIER-BODY-PRIVATE-VAR-SCOPE: a base MODIFIER whose body reads a `private` state var of ITS declaring contract, attached to a function DECLARED IN A DERIVED contract, over-rejects the whole contract. `abstract A9{ address private _owner; modifier onlyOwner(){require(msg.sender==_owner,"no");_;} } contract B9x is A9{ function f() external onlyOwner{...} }` → model importedContractAccepted=FALSE, `TypeError.unknownIdentifier "_owner"`; solc 0.8.35 exit 0, full mixin stack (Ownable2+Pausable2+Guard2) deploys+runs on anvil (deposit/pause/IsPaused revert all correct). BLOCKS THE ENTIRE OZ Ownable/Pausable/AccessControl mixin pattern whenever a leaf contract uses an inherited modifier reading its declaring contract's private var. Root: `ModifierInvocation.checkBodyForCaller` (`TypeCheck.lean:11833-11870`) re-typechecks the modifier body in the CALLING FUNCTION's contract env, whose state-var scope is "own + non-private-inherited" (comment :14040) → the declaring contract's private vars are absent. solc resolves modifier bodies in the DECLARING contract's scope. Fix: typecheck a modifier body in ITS declaring contract's scope (include that contract's privates), not the caller's. Isolation (MATCH): `internal` var OK; modifier used only on a base-declared fn OK; private vars inherited without modifier OK; derived same-name shadow → correctly binds the BASE slot at runtime (gate open at shadow=9, matches solc). Fails at depth 2 + triple-mixin | over-reject | replay | MERGED pending gate (main 1294798, cherry-pick of 96977c6 — modifier body typechecked in DECLARING contract's private scope via CheckEnv.modifierDeclaringPrivateStateVars; full build green; no proof change; full 261-replay green (erc20 lone timeout was spurious contention, confirmed lean=ok solo)) — GATED |
| 2026-07-12 08:0x | 198 | LIBRARY-CALL-ENUM-CALLFORM-ARG: an internal library call with an enum-typed CALL-FORM argument over-rejects the whole contract. `library Lib { enum Mode{Off,Slow,Fast} function isOff(Mode m) internal pure returns(bool){...} }` — `Lib.isOff(Lib.Mode(m))` (explicit conversion), `Lib.isOff(Lib.Mode.Off)` (enum literal), and struct-ctor-nested `Lib.get(Lib.S(Lib.Mode(m),10))` → `TypeError.unsupported "checked executable checked contract T"`; solc+EVM fine. Enum provenance irrelevant (library-own/file-level/other-contract all fail). Isolation (MATCH): enum-typed IDENTIFIER args (local/state/param) to the same lib fn work; enum RETURN from a lib fn works; identical literal/conversion args to a CONTRACT-internal fn work; `uint8(Mode.Fast)` uint arg works — specifically the library-boundary arg handling of enum call-form exprs. Root hypothesis: `FunctionDecl.internalLibraryCallMatches?`→`Parameters.matchArgsAllowingInternalFunctionNamesWithFunctionsEnv?` (`Interface.lean:21430-21445`) and/or boundary arg typing in `internalCallParts?`/hoisting (:14030+) can't type enum conversion/member-literal exprs after enum resolution | over-reject | replay | found (queued — Fable fixer) |
| 2026-07-12 08:0x | 197 | INHERITED-STRUCT-FIELD-ACCESS: any field access on a BASE-declared storage struct from a derived contract over-rejects the whole contract. `contract Base{ struct P{uint a;uint b;} P internal p; } contract C is Base{ function getA()public view returns(uint){return p.a;} }` → model `TypeError.unsupported "checked executable checked contract C"`; solc+EVM run (setA/getA=42). Same for field WRITE, `mapping(uint=>P)` element fields, `P[]` element fields, grandparent-declared vars. KILLS common OZ-style inherited-storage patterns. Root: `ContractDecl.resolveStructs` (`Interface.lean:2116-2130`) builds its typeEnv from `decl.items` state vars ONLY — inherited state variables are invisible, so `p.a` never gets the member→fieldIndex rewrite (:1706-1713) → downstream unsupported. Fix: seed resolveStructs' typeEnv with inherited state vars (walk the C3/base chain). Isolation (MATCH): base accessing its own field + derived calling it; derived whole-struct copy `P memory q=p; q.a`; plain inherited uint256; using-for on inherited struct storage (passes self whole) | over-reject | replay | MERGED pending gate (main 1294798, cherry-pick of 96977c6 — resolveStructs seeded with C3-linearized non-private inherited state vars via resolveStructsInHierarchy + contextual resolveContractSourceTypes? path; full build green; no proof change; full 261-replay green (erc20 lone timeout spurious, confirmed solo)) — GATED |
| 2026-07-12 08:0x | 196 | NESTED-LIBRARY-CALL-TEMP-SHADOW: library call chains ≥3 deep compute garbage (gensym collision). `library M{add/sub/mul(uint,uint) internal} using M for uint256` — `x.add(1).add(2).add(3)` x=10 → model 3, EVM 16; `M.add(M.add(M.add(x,1),2),3)` → 3 vs 16; 4-deep → 4 vs 20; `x.add(1).mul(2).sub(3)` → model Panic 0x11 (spurious), EVM 19. Also as vardecl RHS and as arg to another lib call. Mechanism (core-body dump): the final chain step's receiver reads a hoist temp NEVER assigned (defaults 0 → `0.add(3)`=3, `0.sub(3)`=panic) — the nested-arg hoist temp is named `_sol_internal_call_arg_eval0` at EVERY recursion depth; the inner recursion re-declares the same name in a nested block SHADOWING the outer, so the inner chain's result is assigned to the shadowed inner var and discarded at block exit. Root: `internalCallArgTempName` (`Interface.lean:11959-11960`) = `prefix++"_eval"++index` — index is arg-position only, NO depth/uniqueness component; recursion sites `internalSingleReturnCallExprCore?` (:14105-14150, passes literal 0 at :14116) + `internalTypeConversionSingleReturnUseCore?` (:14518) restart it every level. Fix: thread a monotone depth/gensym counter into the temp name (like the ANF pass's `_sol_hoist_N`). This is a GENSYM-COLLISION in the pre-ANF enumerated hoisters — folds into REARCH-2 WS1 hoister-faithfulness audit. Isolation (MATCH): depth-2 chains OK; plain contract-internal nesting depth-4 OK (different path); chain inside a binary op OK — only whole-RHS/whole-arg library-call position breaks | wrong-value/spurious-revert | replay | found (queued — Fable fixer; part of WS1 hoister audit) |
| 2026-07-12 07:3x | 195 | EMIT-CALL-ARGS-DEFEAT-TWO-PHASE: `emit E2(f(), g(), f())` (y indexed; f/g mutate trace) — solc/EVM evaluates the INDEXED arg first (two-phase: topics reversed, then data forward): topic y=2, data x=21,z=211, trace=211. Model: pure L2R — y=12, data (1,121), trace=121. Double-indexed `emit E3(f(), g())`: EVM topics x=21,y=2 trace=21; model x=1,y=12 trace=12. Root: the interpreter's two-phase emit schedule (R1, `Interpreter.lean:9294-9328`) is DEAD for CALL args — the statement-level call hoister (`Stmt.emitEvent` arm `Interface.lean:13845-13847`, machinery ~13332/20177) pre-evaluates call args into temps in SOURCE (L2R) order, so the schedule only sees pure temp reads. Fix: the emit-arg hoister must pre-evaluate in the TWO-PHASE order (indexed reversed, then data forward). MATCH control: indexed-arg-FIRST emit coincides. R1 residue item verified live | wrong-value | replay | MERGED (main 458f877, 61d9c96 — emitTwoPhaseHoist? applied even to directly-lowerable emits; E2/E3 two-phase topics+data correct, L2R data-emit control preserved; gate 259/259) |
| 2026-07-12 07:3x | 194 | LVALUE-INDEX-KEY-NARROW-NO-CLEANUP: `arr[a+b] = v` / `mp[a+b] = v` / `arr2[1][a+b] = v` / `bs[a+b] = 0x41` with uint8 a=200,b=100 — EVM Panics 0x11 on the key computation (4 Forge tests); model WRITES at index 300 and returns (wrong state + missing panic). Read side `return arr[a+b]` correctly panics (R2 fixed — control). Root: `Expr.toCoreLValue?` (`Interface.lean:6473`) lowers index keys env-less (recorded Stage-D debt ENV_LOWERING_DESIGN.md:145-146). Fix: route lvalue index-key lowering through the env-aware recursion (as R2 did for the read side) | wrong-value/missing-panic | replay | MERGED (main 458f877, 61d9c96 — lvalue index keys env-aware; 4 write cases Panic 0x11 no-write; gate 259/259) |
| 2026-07-12 07:3x | 193 | BUILTIN-ARG-NARROW-NO-CLEANUP: builtin/abi.encode* call ARGUMENTS skip narrow checked arithmetic — `keccak256(abi.encodePacked(a+b))`, `abi.encode(a+b)`, `bytes.concat(bytes1(a+b))` with uint8 a=200,b=100: EVM Panics 0x11; model wraps to 44 and hashes/encodes (h2 encodes un-narrowed 300=0x012c). Also `encodePacked(a*b)` uint8 100*100 and `abi.encode(-a)` int8 -128. Controls: statement-level `uint8 c=a+b` then hash → both panic; 256-bit overflow → both panic; `a-b` negative → both panic (only NARROW-width overflow missed — args computed at 256-bit with 256-bit checks). Root: `Args.toCoreExprs?` (`Interface.lean:6512`) and `Expr.toAbiEncodeArg?` (`Interface.lean:5673`) lower env-less (recorded Stage-D debt ENV_LOWERING_DESIGN.md:141-144). Fix: env-aware arg lowering at declared/inferred arg types. NOTE: Stage-D items A3 (shifts) + A5 (both-hoisted binary) verified CLEAN; fixedBytes-tag observables, condition shapes, conversions, constant-fold guard ALL verified CLEAN | wrong-value/missing-panic | replay | found (queued — batched fixer with #194/#195; R2 Stage-D completion) |
| 2026-07-11 21:5x | 192 | HASH-OF-STORAGE-BYTES-PANIC: `keccak256`/`sha256`/`erc7201`/ext-hash of a BARE storage `bytes`/`string` variable panics 0x0 where solc+EVM return the hash. `bytes stored; store(b){stored=b;} hashStorage()returns(bytes32){return keccak256(stored);}` — store(0x0102030405) then hashStorage → model `RevertData.typeMismatch` (Panic 0); solc+anvil `0x7d87c5ea…76f4` (=`cast keccak 0x0102030405`). Length-independent (5/31/32/33). accepted=true then spurious revert → replay-reachable, COMMON pattern. Root: `Interpreter.lean:6732-6741` `Expr.keccak256` arm does `derefMemoryValueDeep value` (materializes MEMORY refs only) then `value.asBytes?`; a bare storage bytes/string evaluates to `Value.storageRef`/`storagePathRef`, `asBytes?` returns none for storage refs (:211-212) → throw typeMismatch. SAME shape repeated in `Expr.erc7201` (:6742-6748) and `Expr.externalHash` (sha256/ripemd160, :6749-6760) — all 3 hashing builtins share the bug. This is a VALUE-SHAPE ENUMERATION that special-cases memory refs and misses storage refs (same architectural theme as the audit hunt). Fix: before asBytes?, if the value is a storage ref to bytes/string, read storage bytes into List Byte (reader exists ~`State.storageBytes…` :3166-3200, same materialization `bytes memory m=stored` uses); apply uniformly to keccak256/erc7201/externalHash. solc implicitly copies storage bytes/string→memory at the hash-arg boundary. Isolation: `keccak256(m)` (memory copy) OK; `keccak256(abi.encodePacked(stored))` OK; `keccak256(bytes(sstr))` (explicit cast) OK; `keccak256(memArg)` OK — only BARE storage lvalue direct-arg fails. Broad realistic-contract sweep otherwise ALL MATCH (WAD math, enumerable set, ring buffer, packed struct overflow, merkle, signed math, ABI encoder, bitmap, sorting, storage delete/copy/alias) | over-reject/spurious-revert | replay | RESOLVED BY R3 REARCH (main 8f0f3d3 — unified materializeForValueUse at keccak/sha256/ripemd160/erc7201/abi.encode*/concat boundaries; keccak(stored) len 5/31/32/33 + keccak(abi.encode(arr)) + encodePacked(arr) all match cast keccak; gate 256/256) |
| 2026-07-11 21:2x | 191 | EXTERNAL-BINARY-OPERAND-ORDER (external analog of #187, SUSPECT/verify-first — responder-gated): `externalBinarySingleReturnUseCore?` (`Interface.lean:12936`) hardcodes LEFT-to-RIGHT for a binary with an external-call operand — `ext<op>pure` runs the external LHS call first then the pure RHS; `pure<op>ext` evaluates the pure LHS into a temp first then the external RHS call. solc evaluates binary operands RIGHT-then-LEFT (#187, `ExpressionCompiler.cpp:614-615`), so `x = ext.get() + (i++)` (or a pure operand with an observable side effect against the external call) diverges. Could NOT EVM-drive (bare external calls revert without a responder/world in the ownCall harness) → SUSPECT, verify with responder harness. Fix (mirror #187's internal-arm fix): make the external-operand binary arms run the RIGHT operand's effects before the LEFT. Isolation: same construct family as #187 (internal binary, FIXED) — this is the external path the #187 fixer explicitly left out of scope. Eval-order AUDIT otherwise CLOSED: modifier args / internal-call args / low-level-call option order / creation option order all MATCH; every other divergent construct (ctor args, external typed-call args, encodeCall/encodeWithSignature, member-nested LValue index) routes through the #190 L2R root set | wrong-value | replay (responder-gated) | RESOLVED BY R1 REARCH (main 73e1567 — external lhs-call+pure-rhs hoister variants now right-first alongside internal; residual: both-sides-hoisted generic path keeps pre-existing order, documented in EVAL_ORDER_DESIGN.md) |
| 2026-07-11 21:1x | 190 | EVAL-ORDER-L2R-SITES (family with #189): FIVE more sibling-list eval sites use the model's pinned `ChildEvalOrder.yulCompatible=rightToLeft` (`Interpreter.lean:7434`) where solc evaluates LEFT-to-RIGHT → wrong-value with ≥2 non-call side-effecting siblings (`i++`). All EVM-verified (i=0, each sibling `i++`; solc→(0,1), model→(1,0)): (1) EMIT args `emit E(i++,i++)` — `Stmt.emitEvent`→`evalListWithRuntimeOrder …effectiveChildEvalOrder` (`Interpreter.lean:9274`); (2) CUSTOM-ERROR revert args `revert Err(i++,i++)` — `Stmt.revert` (:9268); (3) RETURN-tuple components `return (i++,i++)` — `Stmt.returnValues`→`evalReturnListWithRuntimeOrder` (:9246, worker :7494); (4) BUILTIN arg lists `abi.encode/encodePacked/encodeWithSelector(i++,i++)` + `Expr.tuple`/`Expr.fixedArray` literals — (:6778,6783,6788,6799,6816, list worker :7141); (5) NESTED INDEX base-vs-key `m[i++][i++]=99` (also read path `x=m[i++][i++]`) — `resolveLValueWithRuntimeOrderFuel` index R2L branch evaluates idx before base (:7300-7307) + `memoryRefOrValueWithRuntimeOrder` (:7217-7224). PER-SITE fix (NOT a global flip): R2L is CORRECT and must be PRESERVED for single-index assignment RHS-vs-LHS-ref `arr[i++]=i++` (arr[2]=1, matches solc) and compound `arr[i++]+=i++` — those verified MATCH. Fix: the emit/return/revert-error/abi-builtin/tuple-&-fixedArray LIST evals (sites 1-4) and the index base-vs-key SUB-order in both resolveLValue+memoryRefOrValue (site 5) must use `ChildEvalOrder.leftToRight`; assignment RHS-vs-LHS sub-order stays R2L. Same underlying family as #189 (tuple-RHS-assign :8463/:8483) — fix ALL as one Interpreter.lean eval-order-L2R change. MATCH controls: internal-call args `sink(i++,i++,i++)` L2R (ANF-lifted); single-index assign. Isolation: single-side-effect variants (`return(i++,5)`,`emit E(i++,5)`,`m[i++][0]`) collapse L2R=R2L | wrong-value | replay | RESOLVED BY R1 REARCH (main 73e1567 — ChildEvalOrder deleted; emit two-phase (indexed reversed then data forward, mixed-event Forge-verified), return/custom-error/abi/index all intrinsic L→R; assignment/compound R2L preserved; gate 254/254) |
| 2026-07-11 20:5x | 189 | TUPLE-RHS-COMPONENT-ORDER-RTL: the model evaluates the components of a LITERAL-TUPLE RHS (assignment/vardecl) RIGHT-to-LEFT, but solc 0.8.35 legacy evaluates them LEFT-to-RIGHT. Diverges when ≥2 components have observable NON-CALL side effects (`i++`, `--i`, nested `j=5`) — call components get ANF-lifted so they're unaffected. EVM-verified (forge): `(x,y)=(i++,i)`→solc x=0,y=1 / model x=0,y=0; `(x,y)=(i,i++)`→solc x=0 / model x=1; `(a,b,c)=(i++,i++,i++)`→solc 0,1,2 / model 2,1,0; vardecl `(uint x,uint y)=(i++,i)`→solc 0,1 / model 0,0; `(x,y)=(j=5,j)`→solc 5,5 / model 5,0; hole `(x,,y)=(i++,i++,i)`→solc 0,2 / model 1,0; nested `(a,(b,c))=(i++,(i++,i++))`→solc 0,1,2 / model 2,1,0. Both accept; runtime-only. Root: `Stmt.assignTuple` (`Interpreter.lean:8463-8464`) calls `Expr.evalTupleComponentsRefPreserving context.effectiveChildEvalOrder …`; `effectiveChildEvalOrder` (:7437) defaults to `ChildEvalOrder.yulCompatible`=`rightToLeft` (:7434-7435, the repo's globally-pinned sibling order) → `evalTupleComponentsRefPreservingFuel` (:7401-7406) evaluates rightmost first. solc `TupleExpression` visitor evaluates components in SOURCE order (`ExpressionCompiler.cpp:397` `for(component: components) component->accept`), RHS fully before LHS (:319). Nested path `Stmt.assignTupleNested` (:8483) inherits the same RTL. Fix: pass `ChildEvalOrder.leftToRight` (not effectiveChildEvalOrder) for LITERAL-tuple RHS component eval at :8463 (+ nested :8483); KEEP the right-to-left WRITE order (#132 `writeResolvedRightToLeft` :7626) untouched. MIRROR of #187 but DISTINCT construct on the opposite side (solc: tuple-RHS L→R, binary-operands R→L) — do NOT unify. Isolation: `(a(),b())` direct-call components CORRECT (ANF-lifted L→R); fn-return-RHS `Value.tuple` unaffected (no per-component order); chained `a=b=c=5` correct; array/storage swaps `(arr[0],arr[1])=(arr[1],arr[0])` correct (pure reads). Interpreter.lean fix — INDEPENDENT of the in-flight lowering fixers | wrong-value | replay | RESOLVED BY R1 REARCH (main 73e1567 — tuple-RHS components now intrinsic L→R; (x,y)=(i++,i)→(0,1) Forge-verified; gate 254/254) |
| 2026-07-11 20:3x | 188 | INDEXED-STORAGE-REF-RETURN-PANIC (was "struct-specific"; BROADENED): an internal/library function that RETURNS a storage pointer whose returned expression is an INDEXED or MEMBER path — `return arr[i];` / `return m[k];` / `return o.inner;` — spuriously reverts Panic 0, REGARDLESS of element type (struct, `uint[]`, mapping-value all panic). WHOLE-variable (`return arr;`/`return m;`/`return s0;`) and WHOLE-param passthrough (`refS(S storage x){return x;}`) WORK. NOT struct-specific. Also fires at the return-BINDING itself (`S storage s=refS(1);` with no deref → panic). ROOT (pinned): a storage-ref-returning callee's `return <expr>` is rewritten to `_ret_0 = <expr>; return;` (`Stmt.rewriteStorageReturnAssignmentsFuel` `Interface.lean:12432`); the alias-assignment is lowered at `Interface.lean:17745` which only matches a BARE `Expr.ident` RHS (→ `storageAliasAssignmentCore?` :12374 emitting `storageAliasAssign`/`storageAliasAssignFrom`). An `Expr.index`/`Expr.member` RHS doesn't match → falls through to ordinary value-assignment lowering which LOADS the value via `Expr.storagePath` (`Interpreter.lean:6459 loadStoragePath`), binding a VALUE (not a pointer) into the caller's storage-alias temp → `internalCallAssign`→`assignLocalRefAware?`→`coerceLike?` (`Interpreter.lean:8205`/`1360`) can't coerce value→alias slot → `typeMismatch`=`panic 0` (:302). FIX (interpreter already supports it): extend the storage-alias-return dispatch at :17745 to also match indexed/member RHS — state-var-rooted path (`coreStorageStatePath?` :5955) → emit `Stmt.storageAliasAssignPath name target indexes` (`Interpreter.lean:8364`); ref-local-rooted (`coreStorageRefPath?` :5965) → `storageAliasAssignFromPath` (:8381). Mirrors the WORKING local `S storage s=arr[i]` decl lowering (`Interface.lean:6961-6964`). One change fixes struct + uint[] + mapping-value element returns, at both internal-call and library using-for boundaries. `struct S{uint a;uint b;} S[] arr; function refS(uint i) internal view returns (S storage){return arr[i];} function fnReturn() external returns(uint){ arr.push();arr.push(); S storage s=refS(1); s.a=42; return arr[1].a; }` → model `RevertData.panic 0`; solc+anvil 42. accepted=true then spurious revert (over-accept→wrong-runtime), replay-reachable. Pre-existing, ANF-INDEPENDENT (fails whether or not the returned pointer goes through the ANF hoister). Isolation proving NOT the rearch: `S storage s=arr[1];s.a=42` (direct local struct ref) → model 42 ✓; `S storage s=refS(1);s.a=42` (fn-returned) → PANIC ✗; `refS(1).a=42` (call-as-member-base) → PANIC; read-only `refS(1).a` → PANIC; the identical `uint256[] storage`-returning fn WORKS (77/write-through 55) and `mapping`-ref works — trigger is precisely an internal fn returning `S storage` (STRUCT pointer). Root: internal-call boundary struct-storage-ref return decoding (NOT Expr.anfHoist) — the returned struct storage pointer arrives mis-typed (likely a Value.word / wrong storagePathRef) so member access panics. Adjacent to #116 (ternary storage-ptr local) / #155-156 (library storage-return) / #53 (storage-derived fn-ptr). Common `_positions[id]`-style accessor pattern | over-reject/spurious-revert | replay | RESOLVED BY R3 REARCH (main 8f0f3d3 — storage-alias-return dispatch accepts any storage-ref RHS emitting storageAliasAssign(From)Path; ANF storage-temp fix; coerceLike? repoints refs; arr-elem 42/mapping 88/uint[] 55/o.inner 42/using-for 42 all write-through; gate 256/256) |
| 2026-07-11 20:1x | 187 | BINARY-OPERAND-ORDER-LTR (R1): binary operand evaluation order is LEFT-to-RIGHT in the model but solc is RIGHT-then-LEFT (`ExpressionCompiler.cpp:614-615`) for the common `f() OP g()` shape where BOTH operands are DIRECT internal/library single-return calls → SWAPPED return values in ordinary replay-reachable code. `uint x; rd()=return x; wr(){x=5;return 0;}` — `f(){x=0;return rd()+wr();}` → solc+EVM 5 (RIGHT wr sets x=5 first, then LEFT rd reads 5), model 0; `g(){x=0;return wr()+rd();}` → solc 0, model 5. Pure wrong-value (no storage/event obs needed). Order corroborated via seq-encoding: `a()+b()`→solc 21(b,a)/model 12(a,b); `(a()+b())*(cc()+dd())`→solc 4321/model 1234; same for `- * / & | ^ < > ==`. Root: the ANF pass (`Expr.anfHoist` :13278-13282) hoists correctly RIGHT-then-LEFT but is only the `[]`-base FALLBACK; the pre-existing enumerated two-direct-call binary arms win first and emit LHS-before-RHS: `FunctionDecl.internalTwoSingleReturnCallsCore?` (`Interface.lean:13838`, `firstPrefix;captureReturn first … secondPrefix;captureReturn second`) and `internalBinarySingleReturnUseCore?` non-bool branch (~:14398). Fix: emit RHS call prefix+capture BEFORE LHS in those two arms (keep operand positions), OR retire the two-direct-call arms so the RIGHT-then-LEFT ANF fallback handles them; KEEP the boolAnd/boolOr short-circuit branch LEFT-first (correct). Isolation (pins trigger = both operands DIRECT calls): `id(a())+b()` → 21 CORRECT (LHS non-direct → ANF fires); `id(a()+b())` → 12 WRONG; `a()+b()+cc()` → 123 WRONG. NOT rearch-introduced (pre-existing; rearch flagged R1 as not-Forge-anchored). SIBLING of #91/#28/#132 eval-order family | wrong-value | replay | RESOLVED BY R1 REARCH (main 73e1567, e529a28 — eval order made intrinsic per construct; binary R→L incl. importer call-hoisters right-call-first; rd()+wr()=5 verified vs Forge; the 3 previously-regressed fixtures lean=ok; gate 254/254 green) |
| 2026-07-11 19:3x | 186 | EXTERNAL-SELF-CALL-UNSUPPORTED: an external self-call `this.f()` → `TypeError.unsupported "checked executable … call C"` at lowering. `function getX() external view returns(uint){return x;} function h() returns(uint){ return this.getX(); }` — model unsupported; solc+EVM post-deploy return the value (5); IN A CONSTRUCTOR `y=this.getX()` real EVM reverts (extcodesize-0 self during construction). NOT constructor-specific (plain post-deploy `this.getX()` also unsupported). External SELF-call dispatch (a CALL to address(this)) is unimplemented in the model's lowering/execution — distinct from ordinary external calls to other contracts (responder-answered). Broad ctor/inheritance/C3/modifier area otherwise swept CLEAN (22 scenarios: virtual dispatch during construction, modifier arg lazy-eval + nesting order, state-init-before-ctor-modifier ordering, diamond ctor order, immutable-in-base-read-via-override, 3-level init-then-body ordering, revert propagation — all MATCH). In the external-call/lowering family the rearch touches | over-reject | replay | found (HELD pending rearch/lowering — external self-call execution capability gap; verify if ANF/external-call rearch addresses it, else architectural self-dispatch fix; may need extcodesize-during-construction revert semantics too) |
| 2026-07-11 19:2x | 185 | ARRAY-LITERAL-ASSIGN-UNSUPPORTED: `uint8[2] memory m = [uint8(5), uint8(0)];` (inline FIXED-array literal as a var-decl/assignment initializer) → contract-wide `TypeError.unsupported "checked executable checked contract C"` at execution, even with PURE-literal elements (not narrow-arithmetic-specific). solc+EVM execute it fine (Panic only if an element overflows). Adjacent to the #56 AL1 / #58 AL-EXEC / #87 array-literal work — likely a residual lowering gap for fixed-array-literal in assignment/vardecl position. Distinct from the narrow-cleanup family. May be closed incidentally by the ANF rearch (array-literal element lowering) — verify against post-rearch base | over-reject | replay | CLOSED BY REARCH (main 53f0033 — verified post-rearch: `uint8[2] memory m=[uint8(5),uint8(0)]; return m[0]` now lowers + returns word 5; the ANF call-hoisting pass's re-run of resolveStructs/annotateAbi on normalized statements closed the fixed-array-literal assignment lowering; gate 252/252 green) |
| 2026-07-11 19:2x | 184 | STRUCT-CTOR-FIELD-NARROW-NO-CLEANUP: `struct S{uint8 x;uint8 y;} S st; st = S(a+c, 0);` (a,c uint8, a+c=300) on ASSIGNMENT/VARDECL skips narrow checked-overflow cleanup → model st.x=44 (300 mod 256) where solc+anvil REVERT Panic 0x11 (verified cast call). Also `S memory s=S(a+c,0)`→44, named `S({x:a+c,y:0})`→44. SIBLING of #182/#183 (env-less value lowering drops narrow cleanup). Root: `Interface.lean:1747-1760` `Expr.resolveStructsFuel` `Expr.call (Expr.typeName (Ty.user path)) args` arm rewrites `S(a+c,0)`→`Expr.tuple` with fields wrapped in EXPLICIT casts `uint8(a+c)`; on the assign/vardecl path this tuple lowers via env-less `Expr.toCore?` which treats `uint8(a+c)` as a plain TRUNCATING cast (→44) instead of the env-aware H2 narrow-cast arm (`toCoreAsWithEnv?` ~:7897) that evaluates inner arithmetic at operand width with checked cleanup. Fix: lower struct-ctor tuple RHS via `toCoreAsWithEnv?` (H2 per field), OR in resolveStructs only insert an explicit cast when the field type differs from the arg's own type. Isolation (decisive): DIRECT-RETURN `return S(a+c,0)` correctly Panics (return path routes through toCoreAsWithEnv? — MATCH); `S(uint8(w),0)` w=uint16 300 → 44 (genuine explicit cast, EVM MATCH); plain `st.x=a+c`/`m[k]=a+c` Panic correctly (env-aware assignment-RHS) | wrong-value/missing-panic | replay | MERGED (main 08dfbe3, fix 5631a5e — struct-ctor tuple RHS lowered per-field env-aware; full build 1137; gate 253/253 green) |
| 2026-07-11 19:2x | 183 | PUSH-ARG-NARROW-NO-CLEANUP: `uint8[] arr; arr.push(a+c);` (a=200,c=100 uint8) skips narrow checked-overflow cleanup → model arr[0]=44 (300 mod 256, add at 256-bit) where solc+anvil REVERT Panic 0x11 (verified cast call 0x4e487b71..0011). Also `arr.push(a*c)`, int8 `arr.push(a-b)`, nested `arr2d[0].push(a+c)`→44 — all EVM Panic. SIBLING of #182/#184 (env-less value lowering drops narrow cleanup). Root: `Interface.lean:12223` (+ indexed twin ~:12225) `Expr.call (Expr.member target "push") [Arg.positional value]` arm lowers the arg via env-less `Expr.toCore? storageNames value`, dropping operand-width uintCleanup/intCleanup → add/sub/mul at 256 bits. Fix: look up the array element type from env/storageRefEnv and lower value via `Expr.toCoreAsWithEnv? storageNames env elementTy value` (as assignment/return paths do) so the checked cleanup fires. Isolation: `arr.push(uint8(w))` w=uint16 300 → 44 (explicit truncating cast, EVM MATCH — trigger is IMPLICIT same-narrow checked arithmetic, not narrowing); `arr.push(a)` safe → MATCH | wrong-value/missing-panic | replay | MERGED (main 08dfbe3, fix 5631a5e — push arg lowered via toCoreAsWithEnv? with element type so checked cleanup fires; full build 1137; gate 253/253 green) |
| 2026-07-11 18:3x | 182 | FORINIT-NARROW-COUNTER-NO-CLEANUP: a narrow-int (`uintN`/`intN`, N<256) loop counter DECLARED IN THE for-init skips checked-overflow narrowing on its increment → model completes where solc+EVM Panic 0x11. `for (uint8 i=250; i<300; i++){s++;}` → model returns s=50 (widens i to 256-bit, no overflow); solc 0.8.35 legacy + anvil REVERT Panic 0x11 (i++ overflows uint8 at 255→256; verified `cast call` data 0x4e487b71..0011). Also `uint16` counter + `uint32` bound. Trigger is SPECIFICALLY the counter declared in the for-init: block-declared `uint8 i=250; for(;i<300;i++)` correctly Panics (MATCH). Affects ALL narrowing forms of the for-init var: `i++`, `i+=1`, `i=i+1`, and for-body increments — impossible readback `for(uint8 i=250;i<260;i++)last=i;`→259. Root: narrowing only fires via `Expr.incDecCleanup`/`assignOpCleanupExpr` built by `Expr.toCoreIncDecWithEnv?` (`Interface.lean:7371`) / `toCoreAssignOpWithEnv?` (:7358), both gated on `Expr.abiTyWithEnv? env target` succeeding; when the for-init var's type is ABSENT from the TypeEnv the fallback emits a plain postIncrement/assign whose evaluator `ResolvedLValue.applyIncDec` (`Interpreter.lean:6209`) applies at full 256-bit width with NO cleanup.apply (contrast applyIncDecCleanup :6221 which narrows→Panics). The env-threaded Source→Core lowering `toCoreAsWithEnv?` (`Interface.lean:7861`) for-loop arm does NOT extend the TypeEnv with the for-init varDecl bindings before lowering cond/post/body (block-sequence lowerer DOES per Stmt.varDecl → why block-declared works). Fix: in the forLoop arm extend TypeEnv with the init varDecl bindings (VarBindings.extendTypeEnv) for cond/post/body. Realistic infinite-loop-by-overflow pattern `for(uint8 i;i<N;i++)` N>255. Isolation table: for-init decl (all forms WRONG=50) vs block decl (all Panic ✓) vs plain `uint8 i=255;i++` (Panic ✓) | wrong-value/missing-panic | replay | MERGED (main 08dfbe3, fix 5631a5e — forLoop arm extends TypeEnv with init varDecl bindings for cond/post/body so incDecCleanup fires; full build 1137; gate 253/253 green) |
| 2026-07-11 18:1x | 181 | ADDRESS-NESTED-CONV-CONST-DEPTH3: `address(address(address(0x1234)))` — ≥3 consecutive non-payable `address(...)` conversions whose innermost arg is a CONSTANT (number/hex/arith-folded literal) — accepts at typecheck but fails to lower → `TypeError.unsupported`, POISONING the whole contract (unrelated fns also fail). solc ACCEPTS + runs (identity conversions → `0x1234`=4660). Root: `Interface.lean:4366-4377` `Expr.toCore?` `address(<call>(innerExpr))` `Ty.address _` sub-branch: when `Expr.toCoreAddressLiteral? innerExpr`=none it checks `if Expr.isAddressLiteralCandidate innerExpr then none else toCore? innerExpr`. At depth 3 innerExpr=`address(0x1234)`=`Expr.call (Expr.typeName (Ty.address _)) [literal]`: toCoreAddressLiteral? (:3798) → catch-all untypedNumberLiteralRat? doesn't recognize the address call → none; `isAddressLiteralCandidate` (:3783) returns TRUE via its `Expr.call (Expr.typeName _) [Arg.positional e] => isAddressLiteralCandidate e` recursion → guard fires → arm returns none → contract fails to lower. Depth-2 folds (toCoreAddressLiteral? handles bare literal); depth-3 with a PARAM inner works (isAddressLiteralCandidate false → else-recursion lowers). Fix: in the `Ty.address _` sub-branch, when toCoreAddressLiteral?=none AND innerExpr is itself a nested address conversion `Expr.call (Expr.typeName (Ty.address _)) [_]`, fall through to `Expr.toCore? innerExpr` rather than bailing none (only return none for a bare out-of-range literal). Mirror the payable sibling arm :5043-5060. Isolation: depth-2 literal RET 4660; depth-3 param RET 4660; `uint256(uint256(uint256(5)))`/`bytes32` depth-3 RET (address-specific); `payable(address(address(0x1234)))` RET (payable node dodges). Adjacent to #170 (address folded-const conv) — SAME address-conversion arm family | over-reject | replay | MERGED (main 4cd484e, fix 478a7a9 — added Expr.isConversionCall; the address + payable arms now recurse into a nested address(...) inner instead of bailing none, only bailing for a bare out-of-range literal; full build 1135 jobs; gate 252/252 green; out-of-range still rejected, contract no longer poisoned) |
| 2026-07-11 17:4x | 180 | BYTESN-EQ-LITERAL: `==`/`!=` between a `bytesN` value and a BARE string/hex literal over-rejected — `bytes4 x; return x == "abcd";` / `x != hex"1234";` / `bytes32 x; x == hex"00";`. solc ACCEPTS (implicitly converts the literal to the `bytesN` operand type, compares as value types); model REJECTS at typecheck `TypeError.unsupported "equality on non-value type"` (importedContractAccepted=false). Root: `TypeCheck.lean:7781-7784` eq/ne arm — the convertibility require just above (:7772-7779) already passes via `implicitLiteralFits lhsChecked.ty rhsChecked.source`, but the equality-comparability guard then checks the operands' RAW inferred types: `require (Ty.isEqualityComparable env.types lhsChecked.ty && Ty.isEqualityComparable env.types rhsChecked.ty)`. A bare `hex"1234"`/`"abcd"` is typed dynamic `Ty.bytes`/`Ty.string` → `_ => false` in `Ty.isEqualityComparable` (:3620-3635) → guard fails. solc checks comparability of the COMMON (converted) value type `bytes4`. Fix: make the guard operate on the resolved common value type — treat comparable when one side is a literal that `implicitLiteralFits` the other side's equality-comparable value type (mirror the :7772-7779 require). Isolation: `x < hex"1234"`/`x & hex"1234"`/`x | hex"ffff"` on bytes4 ACCEPTED (ordered/bitwise paths coerce) → bug specific to eq/ne arm; `x == bytes4(hex"1234")` (explicit cast) accepted + correct value (0x12340000→word 1) → trigger is the BARE literal; `bytes memory x == "abcd"` both REJECT (dynamic bytes genuinely no ==) → needs a fixed-bytes value on the other side. No compounding wrong-value (explicit cast path correct). Sibling of the #140-145 bytesN-literal-coercion family (eq/ne context) | over-reject | replay | MERGED (main 483032d, fix c169c37 — eq/ne comparability guard now passes when one operand is a literal that implicitLiteralFits the other's equality-comparable value type; full build 1135 jobs; gate 252/252 green; no over-accept regression) |
| 2026-07-11 17:2x | 179 | STRUCT-FIELD-VS-ATTACHED-FUNCTION member ambiguity: a struct field `x` and a `using L for S`-attached library fn `x` share a name; `s.x` (value) / `s.x()` (call) has TWO candidate members. solc REJECTS (Error 6675 "Member \"x\" not unique after argument-dependent lookup in struct S memory" — `TypeChecker.cpp:3120-3129` fires 6675 when `possibleMembers.size()>=2`, struct member + ADL/attached collected together). Model ACCEPTS (importedContractAccepted=true) and silently resolves: value form `s.x`→field (7), call form `s.x()`→attached fn (107). Root: `TypeCheck.lean:6151-6165` `Expr.member` on `Ty.user`-struct base does `structDecl.fields.find? (·.name==member)` and returns the field type EAGERLY, never consulting the receiver's attached (using-for) fns to detect a same-name collision; call form (~:6788) likewise. Fix: after finding a matching field, enumerate attached fns applicable to the receiver type; if ≥1 exists raise ambiguity (mirror solc's combined-candidate count → 6675). Isolation: attached fn named `y` (no collision) `s.y()`→both ACCEPT (107); same collision but explicit `L.x(s)` (no member access)→both ACCEPT (pins trigger to member-access resolution); plain field access no collision→both ACCEPT | over-accept | accept-only (witness) | found (BACKLOG — accept-only; lower priority than replay-reachable per over-accept-reachability rule) |
| 2026-07-11 17:0x | 178 | ENUM-MEMBER-ACCESS-ENCODEPACKED-WIDTH: `abi.encodePacked(E.C)` (an enum MEMBER-ACCESS literal `E.C`) packs to 32 bytes instead of 1. solc+EVM: `abi.encodePacked(E.C)`=`0x02` (1 byte); `abi.encodePacked(uint8(1),E.C,uint8(9))`=`0x010209`. Model: `0x00..02` (32B) / `0x01`+32B+`0x09` (34B). Both accept; value correct, only PACKED WIDTH wrong → wrong-value, replay-reachable (importedContractAccepted=true; verified via pinned solc 0.8.35 → anvil `cast call`). ROOT (corrected from searcher's abiTy? lead — the member shape never reaches abiTy?): `Expr.resolveEnumsFuel` (`Interface.lean:1279-1281`) rewrites `E.C` into a BARE `Expr.literal (Literal.number "2")`, DISCARDING enum-ness — so downstream `Ty.packedTopWidth` (`:3685`, enum→1 at :3692) never sees an enum type and packs at full-word default (`_ => 0`). Contrast the `E(x)` CONVERSION form: arm :1288-1293 produces `Expr.enumFromUInt maxValue …`, which `Expr.abiTy?` maps to `Ty.uint 8` (:5516) → packedTopWidth 1 (correct). So `E(2)`/param `e`/local `e` all pack `0x02` (MATCH); only the `E.C` member literal diverges. FIX DIRECTION: in the caseIndex arm (:1279-1281) wrap the resolved ordinal as `Expr.enumFromUInt maxValue (Expr.literal (Literal.number (toString index)))` (mirror the E(x) form) so E.C carries enum width everywhere. REGRESSION SURFACE (why this needs a full replay gate, not a blind edit): `E.C` currently folds to a bare constant; wrapping in enumFromUInt may change CONSTANT-FOLDING contexts (`uint8 constant X = uint8(E.C)`, array sizes `uint[uint(E.C)]`, const initializers) — must verify no over-reject regression. Isolation: `abi.encode(E.C)` (non-packed) correct 32B (width irrelevant); ABI/events/bytesN surface otherwise byte-exact swept clean | wrong-value | replay | MERGED (main 07af1a0, fix a0348cc — enumFromUInt-wrap in resolveEnums + numberLiteralRat? passthrough; full build 1133 jobs; gate 250 cases, 246 pass + 4 OZ/reference contention-timeouts re-run serially all lean=ok = 250/250 green) |
| 2026-07-11 10:3x | 177 | STORAGE-REF-ARRAY-ELEMENT-RECHECKS-BOUNDS-ON-DEREF: a `T storage p = arr[i]` pointer to a dynamic-array element, after the array is shrunk (`arr.pop()`), re-checks the array length on EVERY deref → model `Panic(0x32)` where solc+EVM return the (now-zeroed) slot value 0. solc early-binds: bounds-check once at reference CREATION, then raw `sload` with no recheck. Model late-binds: `Value.storagePathRef "arr" [word 0]` re-runs full path resolution incl. the length check (`Interpreter.lean:4180` `State.resolveStoragePathSlot` dynamicArray arm :4187-4191) on each use (`Runtime.loadStoragePath` :4229). Isolation: pop-then-deref → DIVERGE (0 vs Panic 0x32); pop-then-push-then-deref → MATCH (length restored masks it). CONCRETE REPRO OF THE HELD #146 (DANGLING-STORAGE-PTR family, early-bind vs late-bind). Broad storage/mapping/packing sweep otherwise ALL MATCH (mapping slot derivation incl. int8 sign-extended & bytesN keys, nested mappings, dyn-array push/pop/delete tail-zeroing, struct packing byte-identical to cast storage, fixed arrays, bytes/string 31/32 boundary, delete-struct-with-mapping-member) | over-revert | replay | HELD (same architectural issue as #146 — early-bind storage refs; do NOT autonomously fix; strengthens the #146 decision the user is pending) |
| 2026-07-11 10:2x | 176 | BYTESN-STORAGE-VAR-INDEX-PANIC: indexing a `bytesN` STATE VARIABLE — `bytes32 public b=…; function getByte(uint i) returns (bytes1){ return b[i]; }` — reverts `Panic(0x00)` for every in-range index (solc returns the byte) and reverts wrong panic-kind on OOB (model `Panic(0x00)` vs solc `Panic(0x32)` array-OOB). STATE-VAR SIBLING of #175 (same type-env-blind arm, different sub-branch): `Interface.lean:4874-4879` — when `stateNameRuntimeKey? name` = `some key` it UNCONDITIONALLY emits `storageIndex key index`, never checking the var is a `bytesN`; runtime `Runtime.loadStorageIndex` (`Interpreter.lean:4382-4385`) on a scalar/packedScalar layout returns `typeMismatch`=`Panic 0`. The general non-ident arm (:4885-4894) DOES route fixedBytes via `abiTy?`. Fix (fold into #175's general bytesN-ident-index fix): in the `some key` sub-branch, if the state var is bytesN emit `fixedBytesIndex size (storage key) index` (OOB bound = N → Panic 0x32). Isolation: `uint[] arr; arr[1]` MATCH; `bytes bs; bs[1]` MATCH — bug is bytesN-specific. Distinct from #175 (local/param `none` sub-branch) but SAME arm → one fix | wrong-value | replay | MERGED (main 61cef46, folded into #175 fix 22acbb8; gate 249/249 green) |
| 2026-07-11 10:1x | 175 | BYTESN-IDENT-INDEX-PANICS: indexing a `bytesN` LOCAL/PARAM by a bare identifier base — `function f(bytes32 a, uint i) returns (bytes1){ return a[i]; }` — reverts `Panic(0x00)` in the model where solc+EVM return the byte (`a[3]`=0x44). Both accept; model panics only at runtime (replay-reachable wrong-value/over-revert). Root: `Interface.lean:4874-4884` `Expr.toCore?` case `Expr.index (Expr.ident name) index` only special-cases STORAGE state vars (`stateNameRuntimeKey?`); for any other ident it emits a GENERIC `Expr.index` and — being type-env-free (`Executable.Expr.abiTy? [] (Expr.ident "a") = none`) — never emits `Expr.fixedBytesIndex`. The general-base case just below (:4885-4894) DOES detect fixedBytes via `abiTy?` (so `bytes32(a)[i]` works). Generic index read → `Value.index?` (`Interpreter.lean:585-607`) `Value.word` arm → `RevertData.typeMismatch` = `Panic 0` (:302-303). Primitive `fixedBytesIndex?` (:621-628) is itself correct. Fix: in the non-storage sub-branch of the ident-base index arm, consult the local/param type env and emit `Expr.fixedBytesIndex n base index` when the ident is a bytesN (fall back to generic index for array/bytes/mapping locals). Isolation: `bytes4 a; a[i]` also diverges (not width-specific); `bytes32(a)[i]` expression base MATCHES; `fixedBytesIndex?` primitive MATCHES. 59-case arithmetic sweep otherwise all MATCH | wrong-value | replay | built (22acbb8, branch gap/bytesn-ident-index — added a type-directed arm to Expr.resolveStructsFuel: look up ident type via sourceTyWithEnv?, rewrite bytesN `a[i]`→`bytesN(a)[i]` so the correct general index arm handles BOTH local/param AND state var; no Interpreter change (fixedBytesIndex? already Panic 0x32 OOB); 15 forge; full build green. Closes #175+#176) MERGED (main 61cef46, gate 249/249 green) |
| 2026-07-11 09:5x | 174 | ABI-ENCODE-INTERNAL-CALL-ARG: a direct internal call as an argument to the `abi.encode`/`abi.encodePacked` BUILTIN — `return abi.encode(g());` (also var-decl `bytes b = abi.encode(g());` and discard `abi.encode(g());`) — accepts but fails to lower → `TypeError.unsupported`. solc+EVM: `f()` = abi.encoding of uint256(3) = 0x…03. Root: `Interface.lean` three `abi.*` member-builtin arms of `Stmt.toCoreWithInternalCalls?` fall through to env-less `Stmt.toCore?` WITHOUT invoking the call-hoisting walker `Stmt.argPositionHoist?`: return arm :16546-16567 (else :16566-16567 bare `Stmt.toCore?`), var-decl/assign arm :16016-16034, discard/expr-stmt arm :15060. `Stmt.toCore?` lowers each abi.encode arg via `exprToCore?` (:4696) which can't lower an internal call → none. Contrast: `keccak256(abi.encode(g()))` MATCHES because the ident-call return arm routes through the hoister; only the abi.* arms bypass it. `Expr.findArgPosInnerCall?` (:12856) already descends into `Expr.call callee args`, so routing these arms through `argPositionHoist?` (as the tuple-return :16496, ident-call, and newExpr :15390 arms already do) peels the nested call. DISTINCT from #173 (ternary in USER-fn arg, :11612/:14235) — different shape/position/lines. Isolation: `abi.encode(x)` pure local → MATCH; `d(g())` user-fn arg → MATCH; `keccak256(abi.encode(g()))` → MATCH; only direct call inside abi.* builtin arg diverges (also 2nd-of-two-args, call-in-wrapper-in-abi-arg, var-decl, discard) | wrong-value | replay | MERGED (main e6fa5da, general fix 94126b2 routed the 3 abi.* arms through argPositionHoist?/argPositionHoistPrefix? [var-decl intercept = LIST-level single-binding member-call arm ~:17946]; 5/5 forge; 248 cases; gate 248/248 green) |
| 2026-07-11 09:4x | 173 | TERNARY-WITH-CALL-IN-CALL-ARGUMENT: a ternary containing an internal call (`c ? one() : 2`) in a FUNCTION-CALL ARGUMENT position — `double(c ? one() : 2)` — accepts but fails to lower → `TypeError.unsupported` (poisons whole contract). solc+EVM: f(true)=2, f(false)=4. Root: `Interface.lean` `FunctionDecl.hoistDirectInternalCallArgsAux?` (:14235) hoists a call-in-argument only when it matches `Arg.directInternalCall?` (:11612) = ONLY `Arg.positional (Expr.call (Expr.ident name) args)` (a DIRECT call); a `Expr.ternary … (Expr.call …) …` arg returns none → falls to the `| none =>` arm → passed to the residual PURE arg lowering (`Expr.toCoreAs?`) which can't lower a ternary with a non-pure call branch → arg list lowering returns none. Distinct root from #171/#172 (call-in-position/hoister family, cf #131/#147-#151). Fix: in the `| none =>` arm, detect an `Expr.ternary` arg whose branches contain calls and route it through the SAME guarded-branch ternary hoister used for binary operands/conditions (bind ternary→temp, hoist each branch's call into the guarded arm), preserving L-to-R arg eval order. Isolation: ternary-of-calls in RETURN (no outer call) → MATCH (1); assign-to-var-then-call → MATCH; ternary of plain LITERALS as arg → MATCH (2); ternary-with-call as BINARY operand / require cond → MATCH — only ternary-with-call AS a call-argument diverges (both branches, either branch, assignment-RHS-outer-call all diverge) | wrong-value | replay | MERGED (main d643539, 268b72f; gate 247/247 green) |
| 2026-07-11 09:3x | 172 | TUPLE-VARDECL-TERNARY-RHS: a multi-binding tuple variable DECLARATION whose initializer is a TERNARY (conditional) returning a tuple — `(uint a, uint b) = c ? (uint(1),uint(2)) : (uint(3),uint(4));` — accepts but fails to lower at execution → `TypeError.unsupported` (solc+EVM return 3). SIBLING of #171, SAME root/vein: `Interface.lean` tuple-varDecl lowering (`tupleVarDeclCorePieces?` :6504, base arm :6824) only matches literal `Expr.tuple` RHS; `Stmt.toCoreWithInternalCalls?` (:14758) has multi-binding varDecl arms only for CALL-shaped RHS (:16171/16176/16182/16227), no arm for ternary/general expr; catch-all (:17197)→`Stmt.toCore?` multi-binding arm :6873-6875 `| _ => match init with | some _ => none`. The tuple-ASSIGNMENT path works because `tupleAssignmentCore?` (:6489) lowers ANY rhs via `Expr.toCore?` + `assignTuple` — the vardecl path just never routes through it. GENERAL FIX (also closes #171): generalize tuple-varDecl to declare the N locals then reuse `assignTuple targets (Expr.toCore? rhs)` for ANY rhs. Isolation: assignment+ternary → MATCH (3); vardecl with ternary distributed into components → MATCH (3); vardecl from internal call → MATCH (8) — only declaration × non-tuple/non-call tuple RHS diverges | wrong-value | replay | MERGED (main 0a733a5, general tuple-varDecl fix 6bc0b12; forge lane tuple-vardecl-ternary real-EVM 3/7; gate 246/246 green) |
| 2026-07-11 09:1x | 171 | TUPLE-VARDECL-FROM-ABI-DECODE: a multi-binding tuple variable DECLARATION initialized from `abi.decode` — `(uint256 x, uint256 y) = abi.decode(data,(uint256,uint256));` — accepts (matches solc) but the interpreter's source→core lowering returns `none` → replay yields `TypeError.unsupported` instead of solc's value (7 for encode(3,4)). Root: `Interface.lean` `Stmt.toCore?` (:6805) special-cases multi-binding varDecl ONLY when RHS is a literal `Expr.tuple`; other RHS falls to :6854-6856 `bindings@(_::_::_)` `match init with | some _ => none` (unconditionally unsupported). `Stmt.listToCore?` (:7125) likewise only handles `(some (Expr.tuple items))`. The multi-return hoist `nestedTupleRhsHoist…` (:14449) matches only `Expr.call (Expr.ident name)` (internal calls) — never the `abi.decode` member-call shape `Expr.call (Expr.member (Expr.ident "abi") "decode") …`. So decode works in RETURN position (:7096 abiDecodeReturnExprs) and tuple-ASSIGNMENT position (tupleAssignmentCore?) but tuple-DECLARATION-from-decode was missed. Fix: add a `Stmt.varDecl bindings@(_::_::_) (some abi.decode-call)` case that declares the N locals then reuses tupleAssignmentCore?/abiDecodeReturnExprs. Isolation: tuple ASSIGN from decode → MATCH (7); single-var decl from decode → MATCH (9); tuple decl from INTERNAL call → MATCH (33); literal-tuple decl → MATCH — only multi-binding decl × abi.decode RHS diverges | wrong-value | replay | MERGED (main 0a733a5, general fix 6bc0b12 = new tupleVarDeclGeneralCorePieces? mirroring tupleAssignmentCore?, wired into base + internal-call lowering paths; forge lanes abi-decode=7 + dynamic-component (9,22); gate 246/246 green) |
| 2026-07-11 08:4x | 170 | ADDRESS-FOLDED-CONST-CONVERSION: `address(<folded-const-expr>)` over-rejected — `address(1+1)`, `address(2*3)`, `address(0x1234+1)`, even `address(1-1)` (folds to 0). solc ACCEPTS (constant folds to a `RationalNumberType`; `RationalNumberType::isExplicitlyConvertibleTo` Types.cpp:1050-1056 allows `m_value==0 \|\| (nonpayable && !isNegative() && !isFractional() && integerType() && numBits()<=160)` — literal-ness irrelevant). Model REJECTS: `typeConversionLiteralFits` (`TypeCheck.lean:1391-1395`) routes `address false` targets through `Expr.toCoreAddressLiteral?` (`Interface.lean:3752-3769`) which only matches `Expr.literal (address/number/unitNumber)` and returns `none` for any non-literal expr → folded arithmetic fails. Asymmetry: the `uint`/`int` path routes through `toCoreNumericLiteralAs?`/`numberLiteralRat?` which folds, so `uint160(1+1)` is accepted. Fix: fold untyped-number-literal EXPRESSIONS in the address arm (mirror the integer path); accept when the folded value is a non-negative integer `< 2^160` (plus always-allowed 0). Isolation: `address(2)` plain literal both accept; `uint160(1+1)`/`address(uint160(1+1))` both accept; `address(0-1)` negative both reject | over-reject | replay | MERGED (main df085d3, gate 244/244 green; real-EVM forge=ok) |
| 2026-07-11 15:1x | 169 | MUST-OVERRIDE-TWO-BASES-DOMINATION: a contract that must override a function ambiguously defined by two independent base paths is over-accepted when one path's target strictly-inherits the other. `interface I{f;g} interface J{f} abstract A is I,J{f override(I,J)} abstract B is I{g override} contract C is A,B{}` → solc REJECTS (TypeError 6480: C must override `f` — J gives `f` a second target A::f can't singly dominate) but model ACCEPTS. Root: `TypeCheck.lean:10904` `OverrideMembers.hasConflictFor` `dominated` predicate is GLOBAL/path-insensitive — drops unimplemented `I::f` because implemented `A::f`'s origin strictly-inherits `I`, collapsing the `f` set to one member → no conflict. Same flaw in `hasDominatingImplementedFor` (:10915). solc uses a cut-vertex analysis over the override graph (`OverrideChecker.cpp:768 checkAmbiguousOverridesInternal`): with `J::f` in play, `A::f` isn't a single cut vertex, so `f` stays ambiguous. Fix: replace global-domination with per-path cut-vertex reasoning. Isolation: drop `J` → both accept (J required); `C` overrides `f` explicitly → both accept; simple/asymmetric diamonds & override-lists all match | over-accept | accept-only | found |
| 2026-07-11 07:3x | 168 | DELETE-MEMORY-NESTED-REF: `delete s` on a MEMORY struct/array whose members are themselves memory reference types leaves the nested members un-zeroed. `struct S{uint x; uint[] arr;}` `S memory s; s.x=7; s.arr=new uint[](3); delete s; return (s.x, s.arr.length)` → solc+EVM `(0,0)` (delete allocates a fresh fully-zeroed struct), model `(0,3)` (arr still points at the old 3-elem array). Root: `Interpreter.lean` `Value.defaultLike` (:220-232, esp. :231 `Value.memoryRef id => Value.memoryRef id`) returns the SAME pointer; `delete` on a memory-ref local (:6103-6112) stores `value.defaultLike`, which maps over tuple fields but can't deref-and-zero nested `memoryRef` cells (no `Runtime`). Same root hits the memoryCell (:6118-6124) and valueIndex (:6134-6139) delete paths. Fix: runtime-aware delete that rebuilds fresh zeroed nested values (dyn-array→new empty, fixed-array/struct→recursively fresh-zeroed). Isolation: value-only struct delete → (0,0) correct; top-level `uint[] memory` delete → 0 correct; only nested-memory-ref members survive | wrong-value | replay | MERGED (main 2c74c40, gate 243/243 green. v2 fix gap/delete-memory-nested-ref-v2 683dfe3: kept runtime-aware deleteZeroValueDeep but restored the REPOINT (memoryStoreValue+assignLocal?) on the LOCAL delete path so a surviving alias keeps the old cell — reference-delete + delete-memory-nested-ref both lean=ok) |
| 2026-07-11 02:0x | 167 | CREATIONCODE-ANCESTOR: `type(Base).creationCode` / `.runtimeCode` where `Base` is an ANCESTOR (parent/grandparent) of the current contract over-rejected (`TypeError.unsupported "member creationCode"`) — solc accepts (it's `isPure`; the only compile-time bars are `runtimeCode` on a contract with immutables → error 9274, and a genuine bytecode CYCLE, i.e. a contract's OWN code or mutual reference — NOT a plain ancestor). Root: `TypeCheck.lean:5975-5976` guards with `isCurrentOrAncestorContract path` (:1761-1767, true for current AND every ancestor); should reject only the current contract itself (`path == env.currentContract`). Isolation: `type(C).creationCode` in C (self) rejected by both (match); unrelated `type(D).creationCode` accepted by both; only ancestor access diverges. BONUS same-region over-accept (fold into fix): model has NO error-9274 check, so `type(D).runtimeCode` where D has immutable vars is wrongly accepted (accept-only) | over-reject (+ over-accept bonus) | replay | MERGED (main ea8a519, gate 242/242 green) |
| 2026-07-11 01:5x | 166 | OVERLOAD-MEM-VS-STORAGE-LOCATION: two overloads differing only by data location `f(uint[] memory)` + `f(uint[] storage)` over-rejected as `duplicateSignature` — solc treats memory vs storage as DISTINCT overload signatures (a memory-arg call binds the memory one, unambiguous). Root: `TypeCheck.lean:2197-2198` `FunctionSig.sameSignature` compares only name + `params` (the `Ty` list); data location (`paramDataLocations`/`paramStorageRefs`) is ignored, and `uint[] memory`/`uint[] storage` both lower to `Ty.array (uint 256) none` → `ensureNoDuplicateSignatures` (:2303) raises. solc: `asExternallyCallableFunction(false)` (Types.cpp:3771) normalizes ONLY calldata→memory, then `hasEqualParameterTypes` (:3588) compares with location-aware `==` → memory==calldata (duplicate) but memory≠storage, calldata≠storage. Fix: fold calldata→memory in the signature key but keep storage distinct. Isolation: memory+calldata correctly rejected as dup by both (control); single memory overload accepted by both; storage-arg call is ambiguous on both (masks the bug — must call with a memory-only arg) | over-reject | replay | MERGED (main f60f910→ea8a519, gate 242/242 green) |
| 2026-07-11 01:4x | 165 | BOOL-CAST-OF-COMPARISON: `bool(1==2)` (explicit `bool(...)` around a comparison/logical op of untyped-number-literal operands) over-rejected — solc accepts (`1==2` folds to a bool constant, `bool(boolExpr)` is identity). Model `#eval importedContractAccepted`=false with `invalidConversion bool bool`. Root: `TypeCheck.lean` `exprIsUntypedNumberLiteralExpression` binary arm (:1406-1408) classifies ANY `Expr.binary _ lhs rhs` with untyped-number-literal operands as an untyped NUMBER literal — operator-agnostic — so `==`/`<`/`&&`/… (which yield bool) are mislabeled; then `Ty.canExplicitlyConvert` (:1437) preempts the `actual==target` identity fast-path (:1453) and only matches numeric/bytes/address targets → bool falls to `_ => false` (:1452). Fix: restrict that binary arm to arithmetic/bitwise ops (`+ - * / % ** & \| ^ << >>`), exclude comparison/equality/logical. Isolation: `bool(a==b)` (var operands) accepted by both; `uint8(1==2)` rejected by both (bool→uint) — bug specific to bool-target identity conv of a literal-comparison. Wide conversion sub-sweep (enum/int, bytesN/bytesM, int↔bytesN same-width, contract/address, string/bytes, UDVT, literal→bytesN width) all clean | over-reject | replay | MERGED (main 783c9ae→ea8a519, gate 242/242 green) |
| 2026-07-11 00:3x | 164 | EXP-NARROW-BASE-WIDE-EXPONENT: `base ** e` where base is a narrow `uintN`/`intN` (N<256) and `e` has a STRICTLY WIDER type, with the result consumed at a width ≥ the widened common type (implicit widen to e's type, or explicit cast to it) — model returns the un-narrowed value (`uint8 2 ** uint16 8` → `256`) where solc + real EVM revert **Panic 0x11**. solc types the `**` result as the BASE (left-operand) type ONLY (`Types.cpp IntegerType::binaryOperatorResult`, Token::Exp, ~728-746; AST commonType=uint8 + the "ignoring larger type of second operand" warning) and compiles `checked_exp_uint8`. Model `Expr.binaryToCoreWithEnvTyped?` (`Interface.lean:7559`, key `:7568` `commonOperandTyWithEnv?` + `:7578` resultTy=operandTy) treats `**` as symmetric → lowers base at the common (uint16) width → checked-exp/cleanup at width 16 misses the uint8 overflow. Fix: special-case `BinaryOp.exp` (like shl/shr) so operand/result type = left-operand (base) type, exponent lowered at its own type. Isolation: same-width base/exp, literal exponent, uint8 RETURN type, and base-wider-than-exp all correctly Panic/return (match) | wrong-value | replay | MERGED (main a8da44e, gate 239/239 green) |
| 2026-07-10 23:2x | 163 | NEW-CONTRACT-LOCAL-STRUCT-ARRAY: `new P[](n)` where `P` is a CONTRACT-LOCAL struct is over-rejected — solc 0.8.35 compiles+runs it, model returns `importedContractAccepted=false` with `TypeError.expectedType (array (user ["C","P"])) (array (user ["P"]))`. Root: `TypeCheck.lean:7433-7440` `Expr.newExpr` `Ty.array _ none` branch returns the RAW AST element type; every TARGET type is qualified via `Ty.qualifyLocalUserTypes` (`:2573`) at var-decl/return/param, but the `new`-array RESULT type never is, so `["P"]` ≠ `["C","P"]`. Fix: qualify the new-array result element type. Isolation: `new uint[]`/`new uint[][]`/`new bytes[]` OK (no user path); FILE-LEVEL struct `new P[]` OK (path `["P"]` on both sides); only contract-local struct in local-decl or return position diverges. Events/logs area itself clean-swept (topic0 canonicalization, anonymous, LOGn, indexed hashed reference types, non-indexed data — all match real EVM) | over-reject | replay | MERGED (main 001ff55, gate 238/238 green; fix also normalizes struct-ctor field-tys to avoid a #160 regression) |
| 2026-07-10 14:0x | 161 | CALL-POS-BINARY-LHS-BARE: `f() OP <expr>` where LHS is a BARE internal single-return call and RHS contains an internal call but is NOT itself a bare call, with OP not `&&`/`||` (e.g. `h() + i()*2`, `h() + 100*i()`, `h() + (i()+1)`) fails to lower → `toCoreContract? = none` → `TypeError.unsupported`; solc+model-checker both ACCEPT, EVM returns 5. Root: `FunctionDecl.internalBinarySingleReturnUseCore?` (`Interface.lean:13751`), the `some(LHS bare call), none` arm (13854) fallback (13865–13912) only handles `&&`/`||` (added by #131), `| _ => none` (13912) gives up for arithmetic/comparison. Mirror arm `none, some` (13913) handles all ops via `internalExprSingleReturnUseCore?` → asymmetric. Fix: mirror that general handling into the `some,none` arm for non-short-circuit ops. Isolation: `h()+i()` (both bare) OK; `100*i()`/`i()*100` OK; `1+i()*2` (`none,none`) OK; `100*i()+h()` (`none,some`) OK; only LHS-bare + RHS-compound-with-call + non-`&&`/`||` breaks. Distinct from #131/#133 | over-reject | replay | built (7864307, branch gap/callpos-binary-lhs-bare) — **INTENTIONALLY LEFT UNFIXED ON MAIN** as the competition-website submission candidate; fix ready on the shelf, land after dogfooding. (Fixer also caught an extra temp-name collision `h()+100*i()`→300.) |
| 2026-07-10 13:2x | 160 | RECURSIVE-STRUCT-MEM-CONSTRUCT: memory construction of a self-referential struct (`struct Node{uint v; Node[] kids;}`; `Node memory n = Node(1, new Node[](0))`) accepted by solc AND by the model's acceptance predicate (`importedContractAccepted=true`), but any call fails at checked→executable lowering — `Executable.ContractDecl.toCoreWithBasesAndUsing?` (`Interface.lean:23467`) returns `none` → `Checked.lean:230` raises `TypeError.unsupported`. Isolation: declaration-only (G0) & storage use (G1) & non-recursive dyn-array member (H1) & dyn-array-of-DIFFERENT-struct (H2) all execute fine; ONLY memory construction of the self-referential struct breaks lowering. solc `f()`→1. Big struct-construction area otherwise clean-swept (named-arg reorder/order, nested, fixed-array/bytes/bytesN members, push/map/default/array-of-literals). accept-then-fail-to-execute (soundness gap) | over-reject/unimplemented | replay | built (d2da349, branch gap/recursive-struct-mem-construct — NOT landed; root cause: `Ty.resolveStructs` fuel-dependent residual trees for self-ref structs compared unequal in Executable-side `Ty.canImplicitlyConvert`; fixed via fuel-tolerant `Ty.sameNominalType`) — MERGED (main c027a5f, gate 237/237 green) |
| 2026-07-10 13:0x | 159 | SCIENTIFIC-EXP-PLUS: `1e+5` (a `+` sign in a scientific-notation exponent) accepted by model, REJECTED by solc's scanner ("Invalid literal value" — only an optional `-` is allowed in an exponent, never `+`). Root: `parseDecimalExponentSuffix?` (`Interface.lean:2696-2706`) has an explicit `'+' :: rest` branch folding `1e+5`=100000. Fix: drop the `'+'` branch (leading `+` in exponent → none). Everything else numeric-literal/rational clean-swept (exact-rational folding, denominations, underscores, octal, overflow). Witness-only (solc emits no AST for the rejected literal) | over-accept | accept-only (witness) | built (0a7a494) |
| 2026-07-10 12:5x | 158 | USINGFOR-WIDEN-BIND: `using L for uint8` attaching a library `f(uint256 self)` over-rejected — solc requires only that the receiver be IMPLICITLY CONVERTIBLE to the function's first-param type (`Type::attachedFunctions`, Types.cpp:435 `isImplicitlyConvertibleTo(selfType())`), so `uint8`→`uint256` widens and `x.f()` binds; model uses exact width via `Ty.matchesShape` (`Interface.lean:18872` `firstParamMatchesWithInternalFunctions?`; also `:11214` for additional args) which only tolerates the 0/256 alias → member call unresolved → TypeError. Confirmed: solc `g(255)`→256. Directive APPLICABILITY (receiver == target type, exact) is separate & correct; only the function-usability first-param bind is wrong. Receiver WIDER than param (uint256 recv + uint8 param) correctly rejected by both | over-reject | replay | built (4e454ab) |
| 2026-07-10 12:1x | 157 | MODIFIER-STATIC-QUALIFIER: explicit `Base.m` modifier invocation ignores the static-lookup qualifier — model resolves via `pathLast?` (`Interface.lean:52`, drops `Base.`) then `modifierFindByName?` first-match over C3 most-derived-first `dispatchOrder` → runs `Derived.m` (tag=2); solc uses `VirtualLookup::Static` for a qualified target → runs `Base.m` (tag=1). Confirmed: AST `referencedDeclaration`=Base.m + anvil tag=1. Model carries no referencedDecl / static-vs-virtual distinction for modifier invocations (unlike body base-calls via rewriteBaseCalls). Body-`_`/no-`_`/return-postcode/double-`_`/virtual-override all clean | wrong-value | replay | built (b99b84f) |
| 2026-07-10 11:5x | 156 | LIB-STORAGE-RETURN-USE: runtime USE of a library storage-return value — `L.f(s).x` member/index access on the returned storage pointer — fails at interpreter lowering with `TypeError.invalidType (Ty.user [..,"S"])`, even for the INTERNAL library case the checker accepts. Distinct from #155 (acceptance): this is the runtime-lowering path. Surfaced by the #155 fixer; real behavior proven on real EVM via the #155 Forge lane (42/99). Queue AFTER #155 merges (build on merged main) | over-reject/unimplemented | replay | built (a9d168e) — INTERNAL whole-ref case fixed & executes (=42); external/delegatecall + nested-path-return (`return d.m`/`return o.inner`) SURFACED as architectural (storage ptr can't cross ABI boundary; needs #146-style re-basing), gated to stay clean over-rejects. Also noted a separate pre-existing mapping-return-WRITE divergence (untouched) |
| 2026-07-10 11:4x | 155 | LIB-STORAGE-RETURN: public/external LIBRARY function RETURNING a storage reference or mapping over-rejected — `library L { struct S{uint x;} function f(S storage s) public view returns (S storage){return s;} }` accepted by solc, rejected by model. `FunctionDecl.checkHeader` (TypeCheck.lean:11310 storage-ref return, 11331/11334/11337 mapping/non-abi/abicoder-v2 return) are UNCONDITIONAL; the param-side equivalents (11304/11316/11322/11328) are correctly gated `if env.inLibrary`. Return-side missing the same exemption. Sibling of #90/#93 (param side) | over-reject | replay | merged |
| 2026-07-10 11:2x | 154 | DEPLOY-ABSTRACT: model deploys an `abstract` contract (also `interface`/`library`) at the top-level deploy entry; solc forbids ("Cannot instantiate an abstract contract" / empty bin). The `new`-expr site has the guard (`requireCreatableContractDecl` TypeCheck.lean:4998 — `kind==contract && !abstract`) but the deploy/acceptance entry `CheckedProgram.constructContractFrom` (Checked.lean:710) omits it. Dynamic-dispatch/C3/super machinery all clean-swept. Fix guarded all four deploy entries in Checked.lean via `requireCreatableContractDecl`; interface→"Invalid use (contract creation)", library→"Invalid use of a library name" also confirmed non-instantiable | over-accept | accept-only (witness) | merged |
| 2026-07-10 11:0x | 153 | CODEHASH-NOT-DERIVED: `address.codehash` reads only the `accountCodehashes` seed map (`Interpreter.lean:2276`, `getD 0`), never derived from `accountCodes`; a code-bearing account seeded with code but no codehash → model `0x0` vs real-EVM `keccak256(code)`. Also (2nd, latent) post-adoption path returns `keccak256("")` for a present EMPTY account, not EIP-1052 `0x0`. Reachability HINGES on whether the real Forge harness/oracle supplies codehash — verify-first before fix (may be local derive-from-code, may be architectural like #146) | wrong-value | **NOT replay-reachable** | **CLOSED — verify (Opus) proved unreachable: harness never bridges real-EVM account state into the model; codehash is a deliberately-independent hand-seeded recorded fact (`accountCodehashes`). No Forge lane can force `0x0` (author must seed codehash via `cast keccak <runtime>`). Latent internal-inconsistency (EIP-1052 emptiness) needs an account existence/emptiness predicate the model lacks → architectural like #146; not fixed. Documented caveat only.** |
| 2026-07-10 10:1x | 152 | STRINGCONCAT-HEXLIT-UTF8: `string.concat(hex"61")` (valid-UTF-8 hex string literal) rejected — `checkStringConcatArgs` (`TypeCheck.lean:4555`) requires `Ty.isStringConcatArg` (only `Ty.string`) and inspects only `.ty`, never the source expr, unlike `checkBytesConcatArgs` which special-cases string literals via `exprIsStringLiteral`. solc `typeCheckStringConcatFunction` accepts any arg `isImplicitlyConvertibleTo(string)`; a UTF-8-valid hex/string literal converts (`Types.cpp:1290`). Non-UTF-8 (`hex"ff"`) both reject — no divergence there. Fix was TWO sites: typecheck gate + core lowering (`Interface.lean ~4762` widened `allStringConcatArgs`→`allBytesConcatArgs`); added RFC-3629 `bytesAreValidUtf8` | over-reject | replay | merged |
| 2026-07-10 09:57 | 151 | CALL-IN-NESTED-ARG: call nested non-directly inside any arg (`h(g()+1)`, `h(a[g()])`, `h([g()])`) fails to lower — `Arg.directInternalCall?` (:11384) is bare-`f(...)`-only; `boundaryArgDecls?` lowers the rest plainly. Call-position class; shares #147 arg-boundary root | over-reject | replay | merged |
| 2026-07-10 09:57 | 150 | CALL-IN-NEW-ARG: `new T(g())` / `new bytes(g())` args encoded via plain `Args.toAbiEncode?` (:6006), stmt arms (:14875/15597/16125) don't hoist. Call-position class, distinct locus | over-reject | replay | merged |
| 2026-07-10 09:57 | 149 | CALL-IN-STRUCT-CTOR-ARG: `S(g(), h())` — use-hoister (:13382) treats S(...) as fn call, callee lookup fails → none. Call-position class, distinct locus | over-reject | replay | merged |
| 2026-07-10 09:57 | 148 | CALL-IN-ARRAY-LITERAL: `[g(), h()]` elements — `Expr.array` has NO hoisting arm anywhere, only plain `Expr.toCore?` (:4913). Call-position class, distinct locus | over-reject | replay | merged |
| 2026-07-10 09:45 | 147 | CALL-IN-MODIFIER-ARG: value-returning call in a modifier-invocation arg (`f() m(g())`, external/inherited/ctor-modifier too) fails to lower — modifier arg-binding (`Parameter.toStorageAwareCoreArgDecl?`→`Expr.toCore?`) never routes through call-hoisting `Stmt.toCoreWithInternalCalls?`. Call-position class, new site (non-call exprs clean) | **verified-correct** | replay | **NO-FIX: fixer reproduced — model ALREADY lowers `f() m(g())` (bare/nested/inherited/ctor-modifier) correctly; audit was wrong. Pinned with witnesses in 69c55e0** |
| 2026-07-10 09:05 | 146 | DANGLING-STORAGE-PTR-SHRINK: `S storage p = arr[i]; arr.pop()/delete arr; p.x=…` — model re-derives slot on every deref & re-bounds-checks vs CURRENT length → spurious Panic 0x32; solc fixes slot once at bind, accesses freed physical slot (succeeds). Contradicts docs/solc-storage-mapping-pointer-review.md MATCH claim | over-reject/spurious-revert + missed-write | replay | **SURFACED — architectural (storage-ref rep change: bounds-check at bind not deref), awaiting user decision like #125/#129** |
| 2026-07-10 08:45 | 145 | MAPKEY-LIT-FIXEDBYTES: bare bytes/string literal keying a `fixedBytes`-typed mapping (`m["abcd"]` on `mapping(bytes4=>_)`) — key lowered target-blind (`Interface :4789-4817`/`:6287-6298`) → `Value.bytes`, `coerceValue?` :406 has no fixedBytes/Value.bytes case → spurious `typeMismatch` revert. NEW site, same coercion root | wrong-value/over-reject | replay | merged |
| 2026-07-10 08:45 | 144 | EXTFNPTR-CALL-LIT-FIXEDBYTES: external function-pointer value call `fn("abcd")` to a `bytesN` param — args lowered target-blind (`Interface :7877/:7902` `externalFunctionValueCallCore?`) despite paramTys in hand → dynamic bytes not left-aligned word. NEW site, same coercion root | wrong-value | replay | merged |
| 2026-07-10 08:35 | 143 | ENCWITHSELECTOR-LIT-SELECTOR: `abi.encodeWithSelector(0x12345678, ...)` with a bare 4-byte hex-integer-literal selector fails to lower (type-check accepts, but `Interface.lean:4638-4643` guard sees `Literal.number→uint256` not bytesN4). NEW site, same coercion root | over-reject | replay | merged |
| 2026-07-10 08:13 | 142 | PUSH-LIT-FIXEDBYTES: `bytesN[] a; a.push(hex"1234")` — value lowered target-blind (`storageArrayPushPathCore?` :6504 / `storageArrayPushAssignCore?` :6439 / storage-ref push :11699), literal stays dynamic bytes, stored word diverges from solc's left-aligned. NEW site (not #140/#141). require-custom-error confirmed to share #141 | wrong-value | replay | merged |
| 2026-07-10 08:08 | 141 | EMIT/REVERT-LIT-FIXEDBYTES: `emit Ev(hex"1234")`/`revert Bad(hex"1234")` to a `bytesN` event/error param — emit reverts Panic(0), revert leaves dispatch stuck (emit/revert args lower via target-BLIND `Args.toCoreExprs?`; returns are clean via target-aware path). Separate site from #140 | wrong-value/over-reject | replay | merged |
| 2026-07-10 07:41 | 140 | ABIENCODE-LIT-FIXEDBYTES: string/hex/bytes literal to a fixed `bytesN` external/`new` param is ABI-encoded as dynamic bytes (offset+len+data) not solc's single left-aligned word — `Args.toAbiEncode?` ignores the target param type | wrong-value | replay | merged |
| 2026-07-10 07:12 | 139 | ERROR-SELECTOR-COLLISION: same-name error `.selector` across scopes → global flat selectorEnv returns none → contract unreplayable | over-reject | replay | merged |
| 2026-07-10 07:12 | 138 | QUALIFIED-EVENT-SELECTOR: `X.Ev.selector` rejected (contractSelectorMemberTy? omits events; lowering by-name) | over-reject | replay | merged |
| 2026-07-10 06:5x | 137 | QUALIFIED-EVENT-COLLISION: `emit L.Ev(...)` emits contract's topic0 on name collision; over-rejects w/o collision | wrong-value + over-reject | replay | merged |
| 2026-07-10 06:5x | 136 | QUALIFIED-ERROR-COLLISION: `revert L.E(...)` emits wrong selector on name collision | wrong-value | replay | merged |
| 2026-07-10 06:3x | 135 | ARRAY-MUTATION-VS-USINGFOR: push/pop shortcut preempts using-for (Div1 mem/calldata over-reject; Div2 storage-ambiguity over-accept) | over-reject + over-accept | replay | merged |
| 2026-07-10 06:1x | 134 | CONTRACT-ORDERED-CMP: `<`/`<=`/`>`/`>=` on contract/interface operands rejected; solc unsigned-160 compares | over-reject | replay | merged |
| 2026-07-10 06:19 | 132 | TUPLE-WRITE-ORDER: LHS written L→R interleaved; solc resolves all refs then stores R→L | wrong-value | replay | merged |
| 2026-07-10 06:17 | 131 | SHORTCIRCUIT-CALL-POS: both-external `&&`/`\|\|`, external ternary cond, call-in-pure-operand fail to lower | over-reject | replay | merged |
| 2026-07-10 06:17 | 130 | DELETE-EXPR-TYPE: `delete x` typed as operand type not `tuple()` → value-context uses over-accepted | over-accept | accept-only | merged |
| 2026-07-10 05:36 | 128 | ABI-DECODE-EAGER-HEADCHECK: truncated dynamic-element-array head → Panic 0x41 instead of empty revert | wrong-value | replay | merged |
| 2026-07-10 04:49 | 127 | ENCODECALL-OVERLOAD: `abi.encodeCall` resolves overloaded fn-pointer by arity | over-accept | accept-only | merged |
| 2026-07-10 04:48 | 126 | ARRAY-WIDEN-COPY: length-widening fixed-array storage copy of non-integer elements rejected | over-reject | replay | merged |
| 2026-07-10 03:52 | 124 | SLICE-DYN-BASE: calldata slice on dynamically-encoded element type accepted | over-accept | accept-only | merged |
| 2026-07-10 03:30 | 123 | STRUCT-CYCLE-FUEL: by-value struct cycle ≥65 escapes recursion detection | over-accept | accept-only | merged |
| 2026-07-10 03:30 | 120 | REQUIRE-CUSTOM-ERROR: `require(cond, MyError(args))` — model already correct | verified-correct | — | merged |
| 2026-07-10 02:43 | 121 | CONST-ZERO-BYTESN: constant expr folding to 0 not bytesN-convertible (+ mirror overload over-accept) | over-reject + over-accept | replay | merged |
| 2026-07-10 02:43 | 111 | NEG-LITERAL-COMMON-TYPE — model already correct | verified-correct | — | merged |
| 2026-07-10 02:42 | 117 | FRAC-CMP-OVERACCEPT: fractional-vs-fractional literal comparison accepted | over-accept | accept-only | merged |
| 2026-07-10 02:41 | 116 | TERNARY-STORAGE-STATELVALUE: storage-pointer local from ternary over-rejected | over-reject | replay | merged |
| 2026-07-10 01:19 | 115 | CONST-GETTER-PURE-OVERRIDE: constant state-var getter must be pure to override pure base | over-reject | replay | merged |
| 2026-07-10 01:18 | 114 | DEC-ALLOC-MEMPTR: abi.decode allocation guard omits free-mem-pointer + roundUp | wrong-value | replay | merged |
| 2026-07-10 00:35 | 112 | CREATE-SALT-LITERAL: `new C{salt:0}()` — salt must accept any bytes32-convertible | over-reject | replay | merged |
| 2026-07-10 00:35 | 110 | QUALIFIED-STRUCT-CONSTRUCTION: `A.S(1,2)` over-rejected | over-reject | replay | merged |
| 2026-07-09 22:58 | 109 | COMMON-TYPE-LITERAL-MOBILE: literal's mobile type as common type (+ over-reject twin) | wrong-value + over-reject | replay | merged |
| 2026-07-09 22:58 | 108 | ABI-DECODE-ADDRESS-PAYABLE: decoded address must be payable + tuple type arg | wrong-value | replay | merged |
| 2026-07-09 22:57 | 102 | QUALIFIED-MEMBER cluster: qualified constant/enum-value/state-var/error-selector | over-reject | replay | merged |
| 2026-07-09 22:56 | 106 | INHERITED-IDENTIFIER-CLASH: sibling-inherited identifier clash accepted | over-accept | accept-only | merged |
| 2026-07-09 22:56 | 105 | DUP-EVENT-SIBLING: sibling-inherited duplicate events accepted | over-accept | accept-only | merged |
| 2026-07-09 22:54 | 104 | SYNTAX-OVERACCEPT: const div/mod by zero + vardecl as CF body accepted | over-accept | accept-only | merged |
| 2026-07-09 20:48 | 95 | PUSH-FIELD-LVALUE: `arr.push().field=x` / `arr.push()[i]=x` over-rejected | over-reject | replay | merged |
| 2026-07-09 20:47 | 101 | CALLDATA-LAZY-VALUE: eager validation of calldata value-elements | over-reject | replay | merged |
| 2026-07-09 20:47 | — | HEX-DENOM: hex number literal with unit denomination accepted (solc 5145) | over-accept | accept-only | merged |
| 2026-07-09 19:58 | 70 | CALLPOS-FAMILY: hoist every direct-call argument (multi + nested) | over-reject | replay | merged |
| 2026-07-09 19:58 | 94 | STR-OVERACCEPT / PAYABLE-VIS: internal/private payable member functions accepted | over-accept | accept-only | merged |
| 2026-07-09 19:57 | 93 | LIB-STORAGE-PUBLIC-2: non-trivial storage-pointer library args over-rejected | over-reject | replay | merged |
| 2026-07-09 15:48 | 86 | CMP-MIXEDSIGN: opposite-sign folded-literal comparison accepted | over-accept | accept-only | merged |
| 2026-07-09 15:48 | 69 | PRIV-SHADOW: same-signature private fn across base/derived accepted | over-accept | accept-only | merged |
| 2026-07-09 15:12 | 83 | D-OOM-DISPATCH: external-dispatch decoder huge-length → empty revert not Panic 0x41 | wrong-value | replay | merged |
| 2026-07-09 15:12 | 84 | TC-OOM1: try/catch success-path decode collapses Panic 0x41 → empty revert | wrong-value | replay | merged |
| 2026-07-09 14:44 | 81 | DIV-DUP-INH-MOD: base ctor args given twice accepted | over-accept | accept-only | merged |
| 2026-07-09 14:44 | 82 | USINGFOR-BRACE: brace-form using-for doesn't eagerly validate attachability | over-accept | accept-only | merged |
| 2026-07-09 14:07 | 80 | SB-A: non-UTF-8 plain string literal over-rejected (hexValue fallback) | over-reject | replay | merged |
| 2026-07-09 14:04 | 74 | EMIT-QUAL: qualified `emit Base.E(...)` / `emit I.E(...)` not lowered | over-reject | replay | merged |
| 2026-07-09 14:04 | 77 | REVERT-QUAL: qualified custom-error revert (base/self) not lowered | over-reject | replay | merged |
| 2026-07-09 13:46 | 78 | ENCPACKED-LIT: bare number/rational literal in abi.encodePacked accepted | over-accept | accept-only | merged |
| 2026-07-09 13:09 | 73 | EXTCALL-BINARY: external/member single-return call in a binary operand not hoisted | over-reject | replay | merged |
| 2026-07-09 12:29 | 70 | CALL-POSITION: user calls in for/do-while/while conditions not lowered | over-reject | replay | merged |
| 2026-07-09 12:22 | — | ARRAY-BOUNDS: two array acceptance-boundary divergences | over-accept | accept-only | merged |
| 2026-07-09 11:39 | 62 | DEC-OOM: abi.decode oversized dynamic target → empty revert not Panic 0x41 | wrong-value | replay | merged |
| 2026-07-09 09:10 | 63 | GET-STRUCT: struct public-getter member-omission recursed (solc is shallow) | over-accept + wrong-sig | replay | merged |
| 2026-07-09 05:31 | 59 | UNI1: importer emits UTF-16 surrogate pairs for non-BMP unicode → wrong bytes | wrong-value | replay | merged |
| 2026-07-09 05:18 | 58 | AL-EXEC: inline array literals not executed in function bodies | over-reject | replay | merged |
| 2026-07-09 04:37 | 56 | AL1: inline array literal element-widening accepted | over-accept | accept-only | merged |
| 2026-07-09 04:09 | 57 | TUP-IDX: internal-call-indexed tuple-LHS components not hoisted | over-reject | replay | merged |
| 2026-07-09 03:44 | 55 | BASE-CALL: explicit base-qualified call `Base.f()` rejected | over-reject | replay | merged |
| 2026-07-09 03:33 | 53 | FP-EQ-2: internal fn-pointer equality on ternary/storage-derived pointers mis-typed | wrong-value | replay | merged |
| 2026-07-09 03:16 | 54 | MOD-RET: `return <expr>` inside a modifier body accepted | over-accept | accept-only | merged |
| 2026-07-09 03:04 | 52 | EC-CMP: ordered comparison on same-enum operands rejected | over-reject | replay | merged |

## Clean sweeps (areas deep-searched, no divergence found)
| Swept (PDT) | Area | Result |
|---|---|---|
| 2026-07-10 07:5x | Public state-variable getters (signature/key-flattening/return-shape/dispatch) | clean |
| 2026-07-10 07:4x | Immutable variables × inheritance (derived-writes-base, reverse-C3 order, all value types) | clean |
| 2026-07-10 07:3x | Importer AST→source literal translation (numbers/strings/unicode/subdenominations/bytesN) | clean |
| 2026-07-10 09:32 | Multi-dimensional fixed arrays (`uint[2][3]` dim-order: storage row-major, packing, mixed fixed/dynamic, per-dim OOB, aggregates, copy/literal) | clean (8 targets) |
| 2026-07-10 09:27 | Deeply-nested ABI head/tail offsets (3–4 deep, encode+decode, per-element tuple base, deep lazy-defer) | clean (byte-exact) |
| 2026-07-10 09:22 | Explicit conversion matrix (uintN/intN/bytesN/address widths, sign-extend, left/right align, dirty-bit cleanup, allow-reject matrix) | clean (all pairs) |
| 2026-07-10 09:14 | Enum semantics (range-check 0x21, enum↔uint, ABI-as-uint8, mapping-key/getter, 1-byte packing, 256/257 boundaries, decode-empty-vs-storage-panic split) | clean (7 targets) |
| 2026-07-10 09:00 | Storage-pointer local aliasing (basic/rebind/mapping-value/grow/nested/internal-fn boundary/fixed-array) | clean except #146 (dangling-after-shrink) |
| 2026-07-10 08:53 | Entry-point dispatch matrix (receive/fallback selection by calldatasize+value, selector match, trailing-calldata, value-gating, revert branches) | clean (full matrix) |
| 2026-07-10 08:4x | `abi.encodeCall`/`encodeWithSelector`/`encodeWithSignature` (selectors, coercion, verbatim-sig hashing) | clean except #143 |
| 2026-07-10 08:2x | Internal function pointers in aggregates (storage/array/struct/mapping holders, uninit Panic 0x51, packing, virtual/static dispatch, cross-tx) | clean (25 probes) |
| 2026-07-10 07:2x | Function/modifier name-collision resolution (library/free/diamond/using-for/override) | clean (only error/event bare-name pair collides → #136–#139) |
